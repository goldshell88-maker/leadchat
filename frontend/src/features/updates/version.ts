/**
 * Номер последнего выпуска — ОТДЕЛЬНО от текстов журнала изменений.
 *
 * ⚠ ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ. `seen.ts` берёт отсюда одну строку, чтобы понять,
 * видел ли человек последний выпуск, а `seen.ts` импортирует шапка приложения
 * — то есть стартовый чанк. Пока версия вычислялась из `RELEASES[0]`, вместе с
 * ней в стартовый чанк уезжали ВСЕ тексты выпусков: 17 КБ исходника ради одной
 * строки, у всех и всегда, включая тех, кто журнал не открывает.
 *
 * ⚠ ИСТОЧНИК ОСТАЁТСЯ ОДИН. Расхождение этой строки с первым выпуском в
 * `changelog.ts` означало бы, что точка «есть непрочитанное» врёт, — поэтому
 * равенство сторожит тест `src/test/updatesVersion.test.ts`. Добавили выпуск —
 * поправьте здесь же.
 */
export const LATEST_VERSION = "2026.09.09";
