# Точка остановки — 7 августа 2026, вечер

> **УСТАРЕЛО. Актуальная точка — `HANDOFF-2026-08-09.md`.**
>
> Здесь прод на `3e4adc6` и схема `0017`; сейчас `41138a5` и `0023`, а главное
> — с 8 августа в бою два настоящих аккаунта Авито. Читать этот файл как
> описание системы нельзя, он остаётся только как след того дня.

Работа остановлена по просьбе заказчика: «сохрани всё, продолжим позже».
Ничего не потеряно, всё закоммичено и выкачено.

## Состояние на момент остановки

**Прод:** `3e4adc6`, схема на `0017`, все контейнеры healthy, снаружи
`{"status":"ok","db":true,"redis":true}`. Проверено.

**Что выкачено сегодня последними тремя выкатками:**

* `01c09d0` — неделя канала в карточке канала (семь столбиков и «всего / без
  ответа»), метка закрепления в списке, полный аудит;
* `3e4adc6` — починка регрессии: вставка стиля булавки разорвала двухстрочный
  селектор, и жирным стали ВСЕ имена, то есть непрочитанное перестало
  отличаться от прочитанного. Проверено в браузере;
* `c8ce379` — план работ (документы, кода не трогал).

## Три документа, ради которых всё это делалось

| Документ | Что внутри |
|---|---|
| `docs/20-AUDIT-FULL-2026-08-07.md` | Аудит: 17 подтверждённых бед (каждая проверялась на опровержение, три проверку не пережили), 30 мест сделаны правильно |
| `docs/21-ROADMAP-2026-08-07.md` | План: три рубежа — боевой Авито (12,25 дня), пилот 2–3 диспетчера (+21,25), выключение Jivo (+30). Плюс приложения «порядок работ» и «что упущено» |
| `docs/22-INVENTORY-2026-08-07.md` | Опись по коду: 257 пунктов — 137 работает, 71 наполовину, 49 нет |

## На чём именно остановились

Заказчик попросил: «покажи мне подробный план что сделано что нет и тд».

Опись (`docs/22`) собрана и сохранена — это и есть ответ по содержанию.
**Не сделано: страница, на которой это видно целиком** — заказчик просил
показать, а 257 пунктов текстом не показываются. Задел под неё:

* палитра берётся из настоящих токенов продукта (`frontend/src/app/lc-vars.css`):
  фон `#0b0f14`, поверхность `#121821`, карточка `#1a202a`, граница `#2a313b`,
  янтарь срочности `#f59e0b`, зелёный `#22c55e`, красный `#ef4444`,
  бренд `#3cc13b`;
* «наполовину» кодировать ФОРМОЙ (полузалитый квадрат), а не цветом — иначе
  янтарь как акцент и янтарь как статус дерутся между собой;
* шрифты не подключать ссылкой: правила артефакта режут внешние адреса, и
  кириллический файл в странице — это лишние 150–250 КБ. Системный набор плюс
  контраст насыщенности и разрядки;
* мотив взять из самого продукта — шкала срочности, которая растёт снизу вверх.

## Что делать первым, когда продолжим

Задачи заведены и ждут: **#24–41**.

1. **#40 тревоги** — пока каналы отключены, любая следующая проверка пишет в
   лог, который никто не читает. Первым делом на сервере: `crontab -l` —
   расписание указывает на `/srv/leadchat`, а инструкция на `/opt/leadchat`,
   и пока это не сверено, нельзя утверждать, что ночные копии вообще есть.
2. **#24 партиции** — без них боевой аккаунт Авито с историей не подключить:
   загрузка падает на первом же сообщении старше текущего месяца.
3. **#37 результат обращения** — единственный пункт всего плана, который нельзя
   доделать потом: данные не пишутся сегодня, задним числом их не собрать.

## Чего ждём от заказчика

Полный список — `docs/21`, раздел «Решения за заказчиком». Самое раннее:

* доступ в кабинет разработчика Авито (нужен в день ноль, иначе первый рубеж
  встаёт целиком);
* можно ли отдать один канал из девяти под сутки испытаний;
* кого будить ночью — два имени и три события; без адресата включать тревоги
  бессмысленно;
* второй администратор: последнего система удалить не даст, и потеря
  единственного доступа лечится правкой базы.
