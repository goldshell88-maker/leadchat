# Опись готовности LeadChat — 7 августа 2026

Проверено ПО КОДУ, а не по документам: семь агентов прошли по своим участкам и
сверили обе стороны — сервер и интерфейс. Готовая серверная ручка, к которой нет
ни одной кнопки, здесь помечена «наполовину», а не «работает».

Где эта опись расходится с `LeadChat-PLAN.md`, верна опись: тот документ помечал
готовым то, что аудит потом опроверг.

> **Поправка 8 августа.** Опись собиралась по репозиторию — так и было велено, —
> и четыре пункта опирались на файлы-образцы (`.env.prod.example`,
> `deploy/crontab.leadchat`), а на боевом сервере значения другие и правильные.
> Проверено по ssh: вторая копия базы уезжает на амстердамский сервер каждую ночь
> со сверкой sha256, служебный токен и ключ подписи ссылок заданы. Три пункта
> переехали в «работает». Было хуже, чем оказалось.

**Обозначения:** ✅ работает · ◐ наполовину · ✖ нет

**Итого 257 пунктов:** ✅ 140 · ◐ 69 · ✖ 48

По-настоящему готово — 140/257 = 54 %.
Ещё 69 пунктов начаты и не доведены: это те места, где сервер
написан, а кнопки нет, или где функция работает с оговоркой, которую человек заметит.

## Каналы Авито: приём и отправка

Как обращение попадает к вам и как ответ уходит клиенту.

✅ 15 · ◐ 13 · ✖ 8

### ✅ Работает

**Переподключение канала, когда Авито отозвал доступ**

<sub>app/api/routes/avito_connect.py:194-203 и 254-262 — свой state с пометкой reconnect, при авторизации ЧУЖОГО аккаунта ничего не пишется и возвращается ?error=account_mismatch; frontend/src/features/settings/accounts/AccountsPage.tsx:246-262 — красная карточка и кнопка «Переподключить»</sub>

**Кнопка «Обновить токен» на карточке канала**

<sub>app/api/routes/avito_connect.py:352-413 — обновление без похода в Авито, отдельные внятные отказы «доступ отозван» (422) и «уже обновляется» (409); frontend/src/features/settings/accounts/AccountsPage.tsx:159-178, 235-242 — кнопка и три разных текста ошибки</sub>

**Плановое продление токена канала без участия человека**

Внутренняя механика: человек её не видит, но именно она держит девять каналов живыми без ежедневных переподключений.

<sub>app/scheduler/main.py:139 — задание каждые 30 минут; app/services/avito_accounts.py:248-278 — обновляются каналы с истечением меньше 2 часов, ошибка одного не останавливает остальные</sub>

**Приём вебхуков: быстрый и защищённый вход**

Внутренняя механика. Секрет канала в логи не попадает.

<sub>app/api/routes/webhooks.py:42-77 — сверка секрета, потолок тела 1 МБ, ограничение 100 событий в секунду на канал, мгновенный ответ 200 и запись в поток с ограничением длины; в БД шлюз не пишет и в Авито не ходит</sub>

**Сырое сообщение от Авито сохраняется до разбора**

Именно это даёт возможность потом доказать «нам это присылали». Читать его можно только из базы — экрана нет.

<sub>app/workers/inbound.py:218-232 — сырец кладётся в webhook_raw_log ДО разбора, повторные доставки не дублируются; app/scheduler/main.py:96-107 — хранится 30 дней</sub>

**Сообщение клиента доезжает до оператора за секунды**

Проверено пока только текстовыми сообщениями имитатора.

<sub>app/workers/inbound.py:211-265 плюс app/services/inbound.py:241-392 — одна транзакция: клиент, диалог, сообщение, постановка в очередь «Входящие», и только после записи — событие на экраны; проверено на настоящем Postgres в tests/integration/test_inbound_pipeline_pg.py:174</sub>

**Повторная доставка одного и того же вебхука не создаёт второе сообщение**

<sub>app/services/inbound.py:191-219 — вставка с защитой по паре «диалог + идентификатор сообщения Авито»; тест tests/integration/test_inbound_pipeline_pg.py:236</sub>

**Наш собственный ответ не прилетает обратно как сообщение клиента**

<sub>app/services/inbound.py:252-253 — событие, где автор равен владельцу канала, отбрасывается; тест tests/integration/test_inbound_pipeline_pg.py:289</sub>

**Имя клиента подставляется в карточку само**

Вебхук Авито имени не несёт, поэтому имя появляется с задержкой в секунды, а не мгновенно.

<sub>app/services/client_enrich.py:1-60 — отдельная фоновая задача после сохранения сообщения, с запасным путём через список чатов, ошибка Авито не роняет приём</sub>

**Отправка текстового ответа клиенту**

Длинный ответ режется на части по 1000 знаков по границе абзаца или слова — это ограничение мессенджера Авито, не наше.

<sub>app/workers/deliver.py:213-333 — три фазы (проверка под замком, поход в Авито вне транзакции, фиксация итога), пять попыток с нарастающей паузой, уважение к «подождите N секунд» от Авито, один автоматический перевыпуск токена на отказ 401; проверено на настоящем Postgres в tests/integration/test_delivery_pg.py</sub>

**Назначение операторов на канал**

Правило «канал, на который никто не назначен, доступен ВСЕМ, а не никому» записано в одном месте и оттуда берётся и очередью, и эскалацией. Менять состав может только администратор; руководитель видит строку «Операторы: N» без кнопки.

<sub>app/api/routes/avito_accounts.py:66-116 — состояние экрана одним ответом и полная замена набора; frontend/src/features/settings/accounts/AssignOperatorsModal.tsx (347 строк) — экран с галочками и поиском, вызывается с карточки канала</sub>

**Свои каналы видны оператору в профиле**

<sub>app/api/routes/avito_accounts.py:122-134 и frontend/src/features/settings/profile/MyChannelsBlock.tsx</sub>

**Уведомление администраторам «канал требует переподключения»**

Само уведомление доходит до центра уведомлений в приложении. Наружу (Telegram, почта) сегодня не уходит ничего — каналы тревог не настроены, это отдельный участок.

<sub>app/workers/deliver.py:194-207 — не чаще раза в 5 минут на канал; app/services/avito_accounts.py:624-666 — кнопка «Переподключить» прямо под уведомлением, ссылка живёт 10 минут; в интерфейсе — frontend/src/features/notifications/catalog.ts:76 (иконка и человеческая подпись)</sub>

**Проверка канала после выкатки (смоук)**

<sub>tests/smoke/test_smoke.py:315-327 — вебхук без секрета отбивается, вебхук со служебным аккаунтом принимается; :361 — проверка, что ни один канал не в состоянии «требует переподключения»</sub>

**Имитатор Авито для проверок без боевого доступа**

Инструмент сделан добротно и именно им проверена вся система. Но он же скрывает две главные поломки: шлёт только текст (поэтому не видно «undefined» вместо фото) и сеет историю днями, а не месяцами назад (поэтому не видно, что загрузка года падает).

<sub>fake_avito/main.py — полный набор: страница согласия, выдача и одноразовость токенов, подписка и отписка от вебхука, список чатов, история, приём исходящих, а также режимы отказов (401/429/500/медленно) и «не доставить N вебхуков»</sub>

### ◐ Наполовину

**Подключение аккаунта Авито кнопкой (вход через Авито)**

Флоу целиком написан и с обеих сторон, но проверен только против имитатора: на проде в docker-compose.override.yml поднят fake-avito, а боевые ключи разработчика Авито не выданы (.env.prod.example: AVITO_CLIENT_ID=CHANGE_ME). Плюс кнопка «Подключить» показывается и руководителю, а сервер требует права администратора — он получит красный тост вместо объяснения.

<sub>app/api/routes/avito_connect.py:185-287 — выдача ссылки согласия, одноразовый state (GETDEL, 10 минут), обмен кода, чтение профиля, сохранение канала; frontend/src/features/settings/accounts/AccountsPage.tsx:321-346 — кнопка «+ Подключить аккаунт Авито» и разбор возврата (?connected=1 / ?error=…)</sub>

**Одновременный ответ двух операторов не должен «ломать» здоровый канал**

Двое отвечают в одну секунду в момент, когда токен истёк: первый обновляет доступ, второму его сообщение помечается «Аккаунт Авито требует переподключения», админам летит тревога, а через полсекунды канал полностью исправен. Админ идёт чинить работающее, оператор уверен, что ответил.

<sub>app/workers/deliver.py:148-156 — результат обновления токена не проверяется; app/services/avito_accounts.py:212-214 — второй процесс получает False, потому что первый держит замок</sub>

**Подписка на входящие (вебхук) при подключении канала**

Регистрация происходит сама и её итог виден на карточке, но повторить её из интерфейса нельзя: кнопки «зарегистрировать вебхук заново» нет ни в коде фронта, ни на сервере (регистрация вызывается только из подключения и из включения канала). Комментарий в коде обещает такую кнопку — её нет.

<sub>app/services/avito_accounts.py:130-143 — регистрация URL вида /api/hooks/avito/{id}?secret=…, итог пишется как ok/сбой; app/api/routes/avito_connect.py:281-284 — провал регистрации не отменяет подключение</sub>

**Отписка от вебхука (отключить канал от LeadChat)**

Сервер умеет, кнопки нет. Это же и есть кнопка отката при параллельной работе с Jivo: сегодня отписаться от вебхука без разработчика нельзя.

<sub>app/api/routes/avito_connect.py:329-349 — ручка POST /avito-accounts/{id}/disable снимает подписку в Авито и гасит канал; поиск по frontend/src не находит ни одного вызова /disable или /enable</sub>

**Включить канал обратно после отключения**

То же: работает только запросом к серверу, кнопки в настройках каналов нет.

<sub>app/api/routes/avito_connect.py:416-453 — включение с попыткой продлить токен и повторной регистрацией вебхука; в интерфейсе вызова нет</sub>

**Испорченное сообщение от Авито не теряется молча**

Сырец сохранён, но узнать «сегодня три обращения не доехали» нельзя ниоткуда: ни экрана, ни счётчика, ни тревоги. Это буквально «клиент написал, а до людей не дошло, и никто не узнал».

<sub>app/workers/inbound.py:186-192 — после пяти неудачных попыток запись уезжает в «отстойник» webhooks:avito:dlq; поиск по app/, deploy/ и frontend/ показывает, что из этого отстойника никто и никогда не читает — есть только строчка в логе</sub>

**Объявление, по которому пишет клиент, в карточке диалога**

На имитаторе объявление видно всегда. На настоящем Авито поле, скорее всего, будет пустым: дотягивания карточки объявления по номеру в системе нет ни в каком виде.

<sub>app/integrations/avito/adapter.py:156-171 — название, цена и ссылка берутся из вебхука, если они там есть; fake_avito/main.py (шапка) прямо пишет, что настоящий Авито шлёт только номер объявления, а название и цену — нет</sub>

**Экран канала честно показывает ход и срыв загрузки истории**

Значения «сорвалась» не существует. Упало на первой странице — карточка молчит, будто ничего и не начиналось; упало на второй — семь суток врёт «Загружаем историю…».

<sub>frontend/src/features/settings/accounts/AccountsPage.tsx:222-229 — полоса «Загружаем историю… N чатов» с опросом раз в 5 секунд; app/services/avito_accounts.py:589-594 — состояний всего два, «идёт» и «нет», а ключ прогресса живёт 7 суток</sub>

**Догон сообщений, не доехавших вебхуком**

Для уже известных диалогов сделано правильно и проверено. Две оговорки, из-за которых это нельзя назвать «работает»: (1) для чата, которого у нас ещё нет, отсечки нет вовсе — будет выкачана вся его переписка и влита в очередь «Входящие» как новые обращения, со звонком у всех тринадцати; (2) на старом сообщении догон спотыкается о ту же нехватку места в базе, что и загрузка истории.

<sub>app/scheduler/main.py:73-95 — задание каждые 5 минут по каждому активному каналу; app/workers/reconciliation.py:130-165 — отсечка считается по последнему сообщению КЛИЕНТА, а не по последней активности (иначе ответ менеджера прятал бы пропавшее сообщение навсегда); проверено в tests/integration/test_inbound_pipeline_pg.py:303 и :386</sub>

**Фоновый догон бережёт квоту запросов к Авито и умеет продлить токен**

Внутренняя механика. Пока каналов девять и история пустая — незаметно; после загрузки года и при девяти каналах это лишний расход квоты и риск, что догон будет молча падать на просроченном токене.

<sub>app/workers/reconciliation.py:26-88 — ходит в Авито напрямую, без бюджета запросов и без обновления токена на отказ 401; готовый аккуратный обход с бюджетом и автопродлением написан в app/integrations/avito/adapter.py:397-460, но не используется нигде (поиск «AvitoAdapter(» по app/ и tests/ пуст)</sub>

**Каналы другого сотрудника в карточке человека**

Сервер готов, экрана нет: чтобы понять, что ведёт конкретный человек, надо открыть все девять каналов и посмотреть галочки.

<sub>app/api/routes/avito_accounts.py:137-155 — ручка GET /users/{id}/accounts готова и с правами; поиск по frontend/src не находит ни одного её вызова</sub>

**Состояние канала на карточке: токен, вебхук, неделя, операторы**

Показано много и показано хорошо. Оговорка: состояние вебхука живёт в оперативной памяти сервера — после его чистки живой канал будет подписан «не зарегистрирован», а перерегистрировать его кнопкой нельзя (см. пункт про подписку).

<sub>frontend/src/features/settings/accounts/AccountsPage.tsx:41-64, 180-244 — статус точкой И подписью, срок жизни токена, состояние вебхука с временем последнего события и предупреждением «молчит дольше 15 минут», сводка за неделю; app/services/avito_accounts.py:112-127 — состояние вебхука хранится только в Redis</sub>

**Остановка приёма сообщений видна снаружи**

Приём может встать ночью, а внешняя проверка будет отвечать «всё хорошо». Внутренняя механика, но цена — смена, начатая с пустыми «Входящими» при живых клиентах.

<sub>app/api/routes/health.py — возраст последнего принятого вебхука считается (_webhooks_probe), но в список «красных» признаков (_red_flags) не входит</sub>

### ✖ Нет

**Удалить канал из системы**

Ошибочно подключённый канал останется в списке навсегда, его можно только отключить.

<sub>В app/api/routes/avito_connect.py и avito_accounts.py нет ни одной ручки удаления аккаунта — только подключение, disable/enable, обновление токена и операторы</sub>

**Фото, голосовые и видео от клиента в переписке**

Клиент присылает фото шильдика — оператор видит пустой пузырь и строку «📎 undefined · NaN Б» без ссылки. Сейчас не всплывает только потому, что имитатор шлёт один текст; сломается в первый же день настоящего Авито. В ремонте техники фото и есть суть обращения.

<sub>app/integrations/avito/adapter.py:174-183 — вложение сохраняется в сыром виде Авито ({"type": "image", …}); app/services/media.py:344-350 — такие вложения отдаются клиенту «как есть»; frontend/src/features/chats/components/thread/MessageBubble.tsx:53-68 ждёт совсем других полей (вид, ссылка, имя, размер)</sub>

**Длинный ответ не приходит клиенту началом дважды**

Оператор пишет описание запчасти на 1500 знаков, система шлёт его двумя частями; если на второй Авито моргнул — повтор уходит с ПЕРВОЙ части. Клиент читает первую часть дважды, оператор видит один пузырь и не понимает, почему клиент пишет «вы это уже присылали».

<sub>app/workers/deliver.py:136-158 — счётчик частей заводится заново при каждой попытке доставки, а отметка об успехе ставится только после последней части</sub>

**Отправка клиенту файла или фотографии**

Сделано честно: композер гасит кнопку «Отправить» и объясняет заранее (frontend/src/features/chats/components/composer/Composer.tsx:130-136, 328-332), к внутренней заметке файл приложить можно. Но «вот такая деталь, смотрите фото» — ежедневная операция, и её нет.

<sub>app/workers/deliver.py:247-266 — сообщение с вложением честно падает целиком с текстом «Отправка вложений в Авито пока недоступна»; в app/integrations/avito/client.py вообще нет метода загрузки файла</sub>

**Отметка «прочитано» уходит обратно в Авито**

В мобильном Авито у владельца будет расти счётчик непрочитанных, который никогда не погаснет, — даже если оператор ответил в LeadChat.

<sub>В app/integrations/avito/client.py нет метода отметки прочтения; вызовов такого рода нет ни в app/services/read_markers.py, ни в воркерах</sub>

**Клиент видит, кто именно ему отвечает (подпись оператора)**

Если нужно «Марина, ЛидЧат» — этого нет ни в каком виде.

<sub>app/workers/deliver.py:104-116 — в Авито уходит только текст сообщения, имя оператора никуда не подставляется</sub>

**Загрузка переписки за прошлый год при подключении канала**

Подтверждаю вывод аудита: загрузка физически не может завершиться — она падает на первом же сообщении старше текущего месяца. Ни уведомления «История загружена», ни диалогов в системе не будет. Отдельно замечу: на эту задачу нет ни одного теста (поиск по tests/ не находит ни одного вызова backfill_account), а имитатор сеет историю днями назад, а не месяцами, — поэтому поломка и не всплывала.

<sub>app/services/avito_accounts.py:372-402 — историческое сообщение пишется с его настоящей датой; app/scheduler/partitions.py:33-52 и миграция 0002_inbound.py:86-99 создают место в базе только под текущий и следующий месяц</sub>

**Работа на настоящем Авито**

План (LeadChat-PLAN.md, пункт 5.3) оценивает это как «час работы после доступа». По коду это не так: до момента, когда за боевым каналом можно работать, нужно как минимум починить место в базе под историю, разлив старой переписки в очередь и показ фото — иначе первый же настоящий канал даст либо пустую систему, либо очередь, залитую прошлогодними диалогами.

<sub>docker-compose.override.yml:10-12 — «пока не выдан доступ к реальному Messenger API Авито, прод работает на имитаторе»; .env.prod.example:77-78 — ключи CHANGE_ME; переключение описано в docs/RUNBOOK-DEPLOY.md §9 как две переменные</sub>

## Очередь и распределение

Кому достанется обращение и что будет, если его бросили.

✅ 17 · ◐ 13 · ✖ 8

### ✅ Работает

**Очередь «Входящие»: отдельная вкладка со списком ничьих обращений**

Порядок один и не настраивается: дольше всех ждущий всегда сверху.

<sub>app/services/inbox.py — queue_condition/list_inbox (диалог без хозяина, отсортирован «кто дольше ждёт — сверху»); ручка GET /inbox в app/api/routes/inbox.py:105; вкладка «Входящие» первой в ряду со счётчиком — frontend/src/features/chats/components/list/ChatListPane.tsx:359-377. 194 юнит-теста участка проходят.</sub>

**Кнопка «Принять диалог»: из тринадцати получает ровно один**

<sub>app/services/inbox.py:625-677 — один UPDATE ... WHERE claimed_by_id IS NULL и проверка числа строк, плюс ровно один повтор на случай «диалог освободили»; проигравший видит имя занявшего. Проверено настоящим параллелизмом в tests/integration/test_inbox_race.py. Кнопка — frontend/src/features/chats/inbox/InboxDecisionBar.tsx.</sub>

**Кнопка «Отклонить» и возможность забрать отказ назад**

Отказ личный: у остальных двенадцати диалог остаётся в очереди — это задумано, а не недоделка.

<sub>app/services/inbox.py — decline (строка берётся FOR UPDATE, отказ хранится в declined_by, повтор идемпотентен) и undo_decline; тост с кнопкой «Вернуть» — frontend/src/features/chats/inbox/useInbox.ts:164-187.</sub>

**Время ожидания и шкала срочности в строке очереди**

<sub>app/services/inbox.py — inbox_item отдаёт waiting_seconds/waiting_human/offered_at; frontend/src/features/chats/components/list/WaitGauge.tsx рисует заливку (5 и 15 минут — пороги), число тикает от общих часов списка (shared/lib/waiting.ts).</sub>

**Автораспределение: обращение само уходит самому свободному оператору**

По умолчанию выключено (app/core/config.py:55) — включается тумблером в настройках, и это осознанно: в Jivo автораздачи нет.

<sub>app/services/distribution.py pick_assignee (наименее загруженный, при равенстве — кому дольше не доставалось) + вызов внутри транзакции приёма в app/services/inbound.py:350-355 + системная запись в ленту и запись в журнал (inbox.assign_by_distribution).</sub>

**Экран настроек автораздачи: включить/выключить и потолок диалогов на человека**

<sub>frontend/src/features/settings/distribution/DistributionTab.tsx (тумблер, число, «Без ограничения», сохранение по кнопке) + GET/PATCH /settings/distribution в app/api/routes/settings.py:50-59 + проверки границ в app/services/app_settings.py.</sub>

**Автораздача отдаёт только тому, кто назначен на этот канал и сейчас в сети**

<sub>app/services/distribution.py:156-205 eligible_candidates — три условия подряд: операторы канала (то же правило, что у очереди), присутствие из Redis, потолок. Не нашлось никого — обращение остаётся в очереди.</sub>

**Возврат в очередь диалога, который система раздала, а человек не тронул и ушёл**

Фактическая задержка больше трёх минут: присутствие гаснет не сразу (TTL 90 с плюс отсрочка 30 с на переподключение). Порог в настройках не выведен.

<sub>app/scheduler/jobs/reclaim.py — выдержка 3 минуты (RECLAIM_AFTER_MINUTES, строка 72), три обязательных условия, страховка проверкой по факту переписки, кадры inbox:new и conversation:updated строго после commit'а; задача зарегистрирована в app/scheduler/main.py:161.</sub>

**Эскалация руководителю «сотрудник не отвечает клиенту 30 минут»**

<sub>app/scheduler/jobs/awaiting.py:170 — вторая ступень с именем оператора; вид conversation.no_reply заведён в каталоге фронта с иконкой и подписью «Диалог без ответа» (frontend/src/features/notifications/catalog.ts:94).</sub>

**Сторожа не отбирают диалог у того, кто в сети или уже отвечал клиенту**

Присутствие смотрится мгновенным снимком, «момента ухода» в системе нет: у оператора, у которого ровно в эту минуту моргнула сеть, диалог могут забрать.

<sub>app/scheduler/jobs/awaiting.py:185-188 и app/scheduler/jobs/reclaim.py:148-155 — обе проверки в обоих сторожах, причём «уже отвечал» проверяется по факту исходящего сообщения, а не по отметке, которую можно забыть снять.</sub>

**Передача диалога с подтверждением получателя**

Сразу после нажатия «Передать» тост говорит «Диалог передан. Ответственный: <вы сами>» — на самом деле диалог только предложен (frontend/src/features/chats/hooks/useConversationActions.ts:130).

<sub>app/services/transfer.py — пока предложение висит, ответственный НЕ меняется; принятие/отказ/таймаут через одну точку. Полоса с двух сторон — frontend/src/features/chats/components/thread/TransferBar.tsx; уведомление получателю обязательным шагом — app/api/routes/conversations.py:686-697; окно выбора — card/TransferDialog.tsx.</sub>

**Непринятое предложение передачи снимается само через 15 минут**

<sub>app/services/transfer.py:157 expired_condition (TRANSFER_TIMEOUT_MINUTES = 15) + app/scheduler/jobs/reclaim.py:210 expire_transfers, задача зарегистрирована; кадр conversation:updated с transfer: null гасит полосу у обоих.</sub>

**Позвать коллегу в диалог, не отдавая его**

<sub>app/services/participants.py — ответственный не меняется, позванный получает уведомление и видит диалог в «Моих» (mine_condition); кнопка «Позвать коллегу» в шапке ленты (frontend/src/features/chats/components/thread/ThreadActions.tsx:70) и окно card/InviteDialog.tsx.</sub>

**Ручная разгрузка очереди от зависших, с предпросмотром**

У остальных операторов закрытые строки останутся висеть в очереди до перезагрузки страницы: кадр после разгрузки не везёт ни строки, ни автора (app/api/routes/inbox.py:447-451), и обработчик на фронте на нём спотыкается.

<sub>app/services/queue_cleanup.py — условие проверяется в самом UPDATE с RETURNING, поэтому клиент, написавший в момент уборки, не закрывается вместе со своим сообщением; в каждый диалог пишется строка с причиной. Кнопка «Разгрузить…» на вкладке очереди и модалка с числом и именами — frontend/src/features/chats/inbox/UnloadQueueDialog.tsx.</sub>

**Статусы диалога «Новый / В работе / Закрыт» и смена статуса**

Закрытый диалог руками в «Новый» не вернуть — только «в работу»; в «Новый» он возвращается сам, когда клиент напишет.

<sub>app/services/conversations.py change_status (запись в ленту, две записи в журнал, снятие отметки автораздачи) + селектор статуса и отдельная кнопка «Закрыть» в шапке ленты — frontend/src/features/chats/components/thread/ChatThreadPane.tsx:361-389.</sub>

**Автоматическое «в работе» при первом ответе оператора**

<sub>app/services/messages.py:280-327 — первый ответ ставит ответственного и переводит диалог в «в работе», причём в журнале различаются «принял из очереди» и «ответил первым»; если диалог стоял в очереди, проставляются те же поля, что и кнопкой «Принять».</sub>

**Клиент вернулся в закрытый диалог — тот сам встаёт в очередь заново**

<sub>app/services/inbound.py:289-297 — статус обратно в «новый», ответственный снимается, enter_queue обнуляет прошлые отказы и ставит свежее время ожидания, в журнал пишутся два события.</sub>

### ◐ Наполовину

**Пометка «от диалога отказались все» и тревога администраторам**

В журнале уведомлений тип этой тревоги показывается технической строкой «🔔 conversation.unclaimed» — без иконки и человеческой подписи.

<sub>app/services/inbox.py:942 _escalate + отдельный счётчик escalated в inbox_counts; чип в строке и «залитая» шкала срочности — ConversationListItem.tsx / WaitGauge.tsx. Но вида conversation.unclaimed нет в каталоге фронта frontend/src/features/notifications/catalog.ts.</sub>

**Очередь показывает только каналы этого оператора**

Пока страницу не перезагрузить, в очередь падают со звуком обращения чужих каналов; тем же путём возвращается диалог, от которого этот оператор уже отказался.

<sub>Сервер сужает выборку правильно: app/services/inbox.py:104 visible_queue_condition + account_operators.visible_accounts_condition. Но живой кадр inbox:new рассылается всем подряд — в app/ws/hub.py:169 _allowed фильтра по каналу нет, а frontend/src/shared/realtime/applyWsEvent.ts:276 applyInboxNew проверяет только право «отвечать клиентам».</sub>

**Возвращённый в очередь диалог сохраняет своё время ожидания**

Клиент, написавший в 10:00, после возврата встаёт в конец очереди с нулём — ниже тех, кто написал минуту назад. То есть ждущий дольше всех прячется, а не выделяется.

<sub>app/services/inbox.py:310 return_to_queue — offered_at = offered_at or now, для принятых руками метка честно сохраняется. Но app/services/inbox.py:255 assign_by_distribution обнуляет offered_at, поэтому у диалога, розданного автоматически и потом возвращённого сторожем, ожидание считается заново.</sub>

**Снять с диалога ответственного и вернуть его в общую очередь**

Руководитель не может снять ответственного с зависшего диалога — только переназначить его конкретному человеку.

<sub>Сервер умеет: POST /conversations/{id}/assign с assignee_id=null возвращает диалог в очередь (app/services/conversations.py:941-952) и рассылает кадр inbox:released. В интерфейсе окно «Передать диалог» (frontend/src/features/chats/components/card/TransferDialog.tsx) умеет только выбрать человека; вызова с пустым получателем по frontend/src нет.</sub>

**Фильтр очереди по каналу**

Выбрал канал, стоя во «Входящих», — очередь молча закрылась и открылся обычный список. Саму очередь по каналу отфильтровать нельзя.

<sub>Сервер принимает GET /inbox?account_id=… (app/api/routes/inbox.py:107). Фронт этот параметр не шлёт вовсе (frontend/src/features/chats/inbox/api.ts:40), а выбор канала в шапке идёт через setFilters, который выключает очередь (frontend/src/shared/stores/chatUiStore.ts:91).</sub>

**Автораздача пробуется только в момент, когда диалог встаёт в очередь**

Обращение, пришедшее ночью или в пересменку, когда в сети никого не было, останется в очереди навсегда — когда смена выйдет, само оно никому не уедет, его нужно взять руками.

<sub>app/services/inbound.py:287 — entered_queue = conv_created and not backfill; распределение вызывается только при entered_queue (inbound.py:350). Для диалога, уже стоящего в очереди, попытка не повторяется никогда.</sub>

**Личное напоминание оператору «Клиент ждёт ответа N минут»**

Главное уведомление смены приходит без иконки и человеческой подписи — в журнале тип выглядит как «conversation.awaiting_you». И оно не сработает вовсе, если отправка ответа сорвалась: отметка ожидания гасится в момент нажатия «Отправить» (app/services/messages.py:413) и при провале не возвращается (app/workers/deliver.py:172 _fail).

<sub>app/scheduler/jobs/awaiting.py — REMIND_AFTER = 15 минут, задача раз в минуту, зарегистрирована. Но вида conversation.awaiting_you нет в каталоге фронта frontend/src/features/notifications/catalog.ts.</sub>

**Возврат в очередь диалога, брошенного ушедшим домой оператором**

На пересменке клиент ждёт вдвое дольше собственного правила: написал в 21:05 — во «Входящих» у заступившей смены диалог появится в 21:35. До этого единственные сигналы уходят человеку, который уже уехал домой, и руководителю, который ночью не работает.

<sub>app/scheduler/jobs/awaiting.py:166 — проверка «if waited < ESCALATE_AFTER: continue» стоит ВЫШЕ возврата, поэтому возврат случается через 30 минут, а не через 15.</sub>

**Зелёная точка «в сети» в окне передачи и приглашения**

Точка протухает: можно передать диалог человеку, который уже вышел, и узнать об этом только через 15 минут, когда предложение отвалится по таймауту.

<sub>Сервер считает is_online из Redis (GET /users/assignable). Фронт кэширует ответ на минуту — frontend/src/features/chats/hooks/useConversationActions.ts:114 (staleTime 60_000), а событие presence:online в веб-версии просто игнорируется (frontend/src/shared/realtime/applyWsEvent.ts:617, ветка default).</sub>

**Возврат диалогов в очередь при увольнении сотрудника**

Сервер умеет, нажать негде. И даже когда нажмут — диалоги вернутся только в базе: на экранах у людей они появятся после перезагрузки страницы. Отключение сотрудника (в отличие от удаления) диалоги НЕ возвращает — это записанное решение.

<sub>app/services/users.py:531-544 — при удалении все открытые диалоги человека возвращаются в очередь, в журнал пишется сколько. Но ни одного WS-кадра после этого не публикуется, а кнопки «Удалить сотрудника» в интерфейсе нет: в frontend/src/features/settings/team/TeamMembersTab.tsx нет ни «Удалить», ни вызова DELETE.</sub>

**Ночная автоуборка очереди от брошенных обращений**

Уборка идёт каждую ночь, и ни выключить её, ни поменять срок из интерфейса нельзя. Её уведомление inbox.cleaned в журнале тоже выглядит технической строкой — вида нет в каталоге фронта.

<sub>app/scheduler/jobs/cleanup.py — ежедневно в 07:20 МСК, срок молчания 30 дней, включено по умолчанию (app/services/app_settings.py:82), администраторам уходит уведомление. Но ручки настроек нет: в app/api/routes/settings.py только distribution, экрана в frontend/src нет вовсе.</sub>

**Клавиши очереди: Ctrl+R (или Ctrl+Enter) — принять, Ctrl+Backspace — отклонить**

Ctrl+Backspace перехватывается и в поле ввода — проверка «человек печатает» стоит ниже по коду, поэтому привычное для Windows «удалить слово» не работает. И при переборе диалогов клавишами список не прокручивается: в ChatListPane.tsx у виртуализатора нет ни одного scrollToIndex, так что с шестой карточки подсветка уходит за нижний край, а дальше загруженных 50 строк клавишами не уйти.

<sub>frontend/src/features/hotkeys/useChatHotkeys.ts:151-190 и подписка в InboxDecisionBar.tsx:28-40 (повторное нажатие пока запрос в полёте гасится).</sub>

**Руководитель видит очередь, чтобы разобрать затор передачей**

Руководитель и наблюдатель очередь в интерфейсе не видят вовсе, хотя сервер её им отдаёт и весь замысел эскалации на этом построен.

<sub>Сервер отдаёт GET /inbox любому с правом читать диалоги, и это записано в шапке app/api/routes/inbox.py:25-32. Но вкладка «Входящие» на фронте скрыта у всех, кроме тех, кто может принимать — frontend/src/features/chats/components/list/ChatListPane.tsx:167.</sub>

### ✖ Нет

**Кнопка «вернуть диалог в очередь» (принял не тот)**

Ошибочно принятый диалог оператор может либо вести сам, либо искать конкретного человека и передавать лично.

<sub>Сервер готов целиком: app/services/inbox.py:987 release + POST /conversations/{id}/release (app/api/routes/inbox.py:357) со схемой ответа и кадром inbox:released. По frontend/src и desktop/src строка «/release» не встречается ни разу — кнопки нет нигде.</sub>

**Статус «отошёл» влияет на автораздачу**

Ушедший на обед с открытым ноутбуком считается самым свободным, и очередь поедет именно к нему.

<sub>app/ws/presence.py:40 хранит единственное значение «online»; «away» существует только в трее десктопного приложения и всего лишь глушит звук локально (frontend/src/platform/index.ts:105, 121-126 — «серверного PUT /presence в API нет»). В distribution.eligible_candidates проверка одна — presence_map.</sub>

**Автораздача не перечитывает лишнего из базы на каждое обращение**

Сегодня незаметно; после загрузки годовой истории Авито превратится в «вебхуки стали приходить с задержкой».

<sub>app/services/distribution.py:147 _last_assigned_at агрегирует max(updated_at) по ВСЕМ диалогам операторов без фильтра по статусу — на каждое входящее сообщение. Рядом _load_by_user фильтр по открытым статусам имеет.</sub>

**Пороги ожидания и возврата настраиваются из интерфейса**

Владелец не может поменять «15 минут» и «30 минут» под свои смены — только правкой кода и выкаткой.

<sub>REMIND_AFTER и ESCALATE_AFTER — константы модуля app/scheduler/jobs/awaiting.py; в app/services/app_settings.py в SPECS всего четыре ключа (распределение ×2, уборка ×2). Ни ручки, ни экрана.</sub>

**Тревога «в очереди висят непринятые обращения»**

Обращение, которое просто лежит в очереди и от которого никто не отказывался, не поднимет тревогу ни через час, ни через сутки. Единственная эскалация срабатывает, когда откажутся все до одного.

<sub>Сторож с названием «Очередь входящих не разбирается» (app/scheduler/jobs/watchdog.py:251) на самом деле меряет длину технического потока Redis через _queue_probe, а не число непринятых диалогов. Сторож ожидания (app/scheduler/jobs/awaiting.py:118) смотрит только на ВЗЯТЫЕ диалоги (assignee_id IS NOT NULL).</sub>

**Клавиша Ctrl+T «Передать диалог»**

В шпаргалке клавиш комбинация обещана, нажатие не делает ничего — человек решает, что программа зависла.

<sub>useChatHotkeys.ts:176 вызывает requestTransfer, счётчик transferNonce в frontend/src/features/hotkeys/actionBus.ts:40 исправно растёт — но по всему frontend/src на него не подписан ни один компонент.</sub>

**Видно, где клиент ждёт и чьи диалоги висят без хозяина за столом**

Заступивший в 21:00 диспетчер видит чужие имена и считает, что это чужая работа, — именно это делает недоработку с 30-минутным возвратом невидимой.

<sub>app/services/conversations.py:183 conversation_out отдаёт ответственного как {id, full_name} — признака «в сети» там нет. В таблице диалогов app/services/conversation_table.py:70 и :82 — восемь фильтров и четыре колонки сортировки, ни «клиент ждёт», ни времени ожидания среди них нет.</sub>

**Чем закончилось обращение (выезд назначен / отказ / спам) при закрытии**

Задним числом это не собрать: через месяц на вопрос «сколько обращений стали выездами и какой канал окупается» ответа не будет.

<sub>В app/models/conversation.py нет ни поля результата, ни суммы; поиск close_reason по проекту не даёт ни одного совпадения; queue_cleanup и change_status закрывают диалог без причины.</sub>

## Рабочее место диспетчера

Экран, за которым диспетчер сидит двенадцать часов.

✅ 21 · ◐ 10 · ✖ 5

### ✅ Работает

**Вкладки списка: «Входящие», «Мои», «Все»**

Вкладок именно три. «Новые» и «Закрытые» переехали в фильтр «Статус» — возможности те же, но привычные вкладки исчезли. Среза «покажи, где клиент ждёт» нет ни одного.

<sub>frontend/src/features/chats/components/list/ChatListPane.tsx — кнопка «Входящие» с красным счётчиком плюс две вкладки; серверная выборка вкладок — app/services/conversations.py, _tab_condition (my/mine, all, new, closed, any).</sub>

**Фильтры списка: статус, канал, менеджер**

Фильтр «Менеджер» видят только администратор и руководитель — у диспетчера его нет. Фильтр «Канал» появляется, только если каналов в загруженных строках больше одного.

<sub>ChatListPane.tsx — три Select'а; список каналов собирается из загруженных строк плюс справочник; фильтр «Менеджер» показывается только при праве stats:all.</sub>

**Поиск по диалогам: имя, телефон, текст сообщений**

Ищет честно по всему — включая чужие и закрытые диалоги. По тексту внутренних заметок не ищет намеренно (иначе наблюдатель нашёл бы то, чего ему видеть нельзя).

<sub>frontend/src/features/chats/api.ts — при непустом запросе шлётся tab=any; app/services/conversations.py, _search_condition — имя ILIKE, нормализованный телефон (снимает скобки и префикс 7/8), полнотекстовый поиск по сообщениям. Ctrl+K фокусирует поле (useChatHotkeys.ts).</sub>

**Список не тормозит на большом объёме (виртуализация, подгрузка по 50)**

Оговорка на будущее: превью последнего сообщения для страницы собирается оконной функцией по всей переписке пятидесяти диалогов (app/services/conversations.py, _load_related). Сегодня это незаметно, после загрузки годовой истории Авито станет заметно.

<sub>ChatListPane.tsx — useVirtualizer из @tanstack/react-virtual + useInfiniteQuery, страница 50, догрузка при достижении строки-лоадера.</sub>

**Пометка «диалог передан вам» (⚑) в строке**

<sub>ConversationListItem.tsx — иконка с подсказкой «Диалог передан вам»; признак живёт в Redis по человеку (app/services/conversations.py, TRANSFERRED_KEY) и гаснет при открытии диалога.</sub>

**Закрепить диалог у себя**

Закрепить можно только диалог, который ведёшь сам; потолок — 7 штук; в очереди «Входящие» закрепление не действует. Всё это осознанные ограничения, но человек про них узнаёт из отказа.

<sub>Кнопка-булавка в шапке ленты (thread/ThreadActions.tsx), оптимистичное переключение (hooks/usePins.ts), серверная часть app/services/pins.py, булавка в строке списка и подъём закреплённых наверх (pins.pin_order).</sub>

**Быстрая передача диалога прямо из строки (по наведению)**

Дублирует такую же кнопку в шапке открытого диалога — два места для одного действия.

<sub>ConversationListItem.tsx — кнопка со стрелкой появляется при наведении и при фокусе с клавиатуры; окно передачи одно на всю колонку (ChatListPane.tsx, TransferDialog).</sub>

**Непрочитанные выделяются в списке**

Разница в начертании — одна ступень (500 против 600), на 12-часовой смене она почти не читается. Реально работает только синий счётчик.

<sub>chat-list.css:340 — у непрочитанной строки имя переходит с веса 500 на 600, превью становится ярче, справа синий счётчик; счётчик персональный (app/services/read_markers.py).</sub>

**Очередь «Входящие»: принять, отклонить, вернуть отказ, «Следующий (N)»**

После принятия очередь остаётся на экране — за следующим не надо возвращаться руками.

<sub>inbox/InboxDecisionBar.tsx (кнопки и клавиши), inbox/useInbox.ts (409 «уже занят» с именем коллеги, тост с кнопкой «Вернуть» после отказа), кнопка «Следующий (N)» в шапке ленты (ChatThreadPane.tsx), двойной звук на новое обращение (inbox/sound.ts).</sub>

**Разгрузить очередь от зависших диалогов**

Кнопка живёт только на вкладке «Входящие» и только у того, кто из неё берёт. Настроить или выключить автоматическую уборку из интерфейса нельзя — это админский участок, там экрана нет.

<sub>inbox/UnloadQueueDialog.tsx — поле «тихо дольше, дней», предпросмотр «сколько и кого закроется» до нажатия; серверная часть — inbox.close_stale.</sub>

**Лента переписки: пузыри, склейка серий, разделители дат, прилипание к низу**

<sub>thread/ChatThreadPane.tsx + thread/MessageBubble.tsx: пять видов пузырей (клиент / оператор / бот / заметка / системная запись), подпись автора только у первого в серии (окно 5 минут), разделитель дня, кнопка «↓ новые сообщения», своё отправленное всегда прижимает ленту вниз, ответ бота — нет.</sub>

**Подгрузка старой переписки при прокрутке вверх**

<sub>ChatThreadPane.tsx, onScroll: при подходе к верху тянется предыдущая страница, позиция экрана восстанавливается по разнице высот; курсорная пагинация с микросекундами — app/services/conversations.py, encode_cursor.</sub>

**Статус доставки своего сообщения и кнопка «Повторить»**

Видно только внутри открытого диалога. Галочка одна и означает «доставлено», а не «прочитано» — Авито про прочтение не сообщает.

<sub>thread/MessageBubble.tsx — часы / галочка / красный крест, под неудавшимся текст ошибки и кнопки «Повторить» и «Удалить»; hooks/useSendMessage.ts — три ветки повтора (десктопная очередь, неотправленный POST, серверный failed).</sub>

**Поле ввода: Enter — отправить, Shift+Enter — перенос, счётчик длины**

<sub>composer/Composer.tsx — автовысота 1→6 строк, счётчик с 800 знаков, предупреждение «длинное сообщение придёт клиенту частями» после 1000, отказ сервера на 4000 показывается плашкой.</sub>

**Черновики по диалогам**

В списке слева начатый и брошенный черновик никак не помечен — увидеть его можно, только открыв диалог.

<sub>shared/stores/chatUiStore.ts — черновики (текст и режим «заметка») лежат в persist-хранилище, переживают переключение диалогов и перезагрузку страницы; при неудачной отправке текст возвращается в черновик (useSendMessage.ts, onError).</sub>

**Кнопки в шапке диалога: закрепить, позвать, передать, пометить нежелательным**

«Передать» и «Позвать» продублированы кнопками в карточке клиента справа — два одинаковых действия в трёхстах пикселях друг от друга. Списка помеченных клиентов в интерфейсе нет (сервер его умеет).

<sub>thread/ThreadActions.tsx — четыре иконки с подсказками и aria-подписями; «закрепить» показывается только для своего диалога, «передать» — по праву управления, пометка — по праву отвечать. Окна: card/InviteDialog.tsx, card/TransferDialog.tsx, card/BlockClientButton.tsx.</sub>

**Кнопка «Закрыть» и переход к следующему диалогу**

Подсказка на кнопке обещает Ctrl+Shift+Enter, а шпаргалка под полем ввода — Ctrl+D. Работают обе, но человек видит два разных обещания. Наблюдателю вместо селекта показывается статичная пилюля.

<sub>ChatThreadPane.tsx — отдельная кнопка «Закрыть» плюс селект статуса; hooks/useConversationActions.ts, useChangeStatus — после закрытия уводит к следующему в очереди и на вкладке «Мои», но не на «Всех».</sub>

**Передача диалога с подтверждением получателя**

Зелёная точка «в сети» в списке получателей — снимок: справочник кэшируется на минуту и сам не обновляется (useConversationActions.ts, useAssignableUsers, staleTime 60 с). Можно передать диалог человеку, который только что ушёл домой.

<sub>thread/TransferBar.tsx — полоса «Борис передаёт вам диалог» с кнопками у получателя и «Ждёт подтверждения: …» с напоминанием «диалог пока за вами» у передающего; серверная двухфазность — app/services/conversations.py, assign_conversation + services/transfer.</sub>

**Позвать коллегу в диалог и состав участников**

<sub>card/InviteDialog.tsx + card/PeoplePicker.tsx, hooks/useConversationActions.ts (useInviteParticipant / useRemoveParticipant); в карточке клиента список «Позваны» с кнопкой убрать и «Выйти из диалога»; на сервере позванные попадают во вкладку «Мои» (app/services/participants.mine_condition).</sub>

**Карточка клиента справа: телефон, объявление, теги, история обращений, заметки**

История клиента запрашивается целиком, без ограничения по количеству (app/services/conversations.py, client_history — order_by без limit). У постоянного клиента после загрузки годовой истории карточка будет открываться заметно медленнее.

<sub>card/ClientCardPane.tsx — имя и рейтинг Авито, телефон ссылкой tel: с копированием и подсветкой на 2 секунды в момент появления, объявление со ссылкой на Авито, теги, ответственный и позванные, история прошлых диалогов клиента с переходом по клику, секция заметок.</sub>

**Колокольчик уведомлений и журнал**

<sub>features/notifications/NotificationBell.tsx (бейдж и список последних), NotificationPanel.tsx, NotificationsPage.tsx (журнал с фильтрами по важности, типу и периоду), CriticalBanners.tsx (критичное плашкой сверху со звуком), wsNotify.ts (доставка по вебсокету). Диспетчеру колокольчик доступен: право conversations:manage есть у роли manager (app/core/rbac.py:48).</sub>

### ◐ Наполовину

**Порядок строк в списке: кто важнее — тот выше**

Значок непрочитанного в строке считается лично по человеку и гаснет правильно, а вот ПОРЯДОК строк — нет. Как только клиент написал хоть раз, диалог навсегда остаётся в «непрочитанной» группе, а внутри неё правило «кто дольше ждёт, тот выше» ставит наверх самые старые диалоги. На практике список получается «отвеченное позавчера сверху, свежее обращение внизу». Аудит этого не поймал, план тоже; тест на сортировку проходит только потому, что в нём колонка проставлена руками.

<sub>app/services/conversations.py:455–491 — сортировка «закреплённые → непрочитанные → негатив → кто дольше ждёт». Но ключ сортировки берётся из колонки conversations.unread_count, а она нигде не обнуляется: поиск по всему app/ даёт только увеличение (inbound.py:273) и запись при загрузке истории (avito_accounts.py:524). В app/api/routes/conversations.py:613 прямо написано, что при открытии диалога колонка «намеренно не трогается».</sub>

**Шкала ожидания в строке списка (цвет + минуты)**

Счётчик считается по непрочитанным, поэтому гаснет в ту секунду, когда оператор открыл диалог, — даже если он не ответил. Диалог, который открыли и бросили, в списке выглядит спокойным. Плюс в светлой теме шкала почти не видна (см. пункт про тему).

<sub>components/list/WaitGauge.tsx — заливка растёт до 15 минут, пороги 5/15 из shared/lib/waiting.ts. Но shared/lib/waiting.ts, waitingForRow: если unread_count = 0, возвращается null.</sub>

**Превью последнего сообщения в строке («Вы: …»)**

«Вы:» приписывается любому исходящему — и ответу коллеги, и ответу бота. Оператор читает «Вы: Здравствуйте, уточните модель» под сообщением, которое написал не он.

<sub>components/list/ConversationListItem.tsx, previewText: подпись «Вы:» ставится по одному признаку direction === "out". app/services/conversations.py:192 — в last_message едут только body, direction и время; ни автора, ни sender_type там нет.</sub>

**Внутренние заметки (жёлтый режим, видны только сотрудникам)**

Сам текст заметок работает. А вот файл к заметке приложить нельзя, хотя интерфейс это прямо советует: плашка в поле ввода говорит «Приложите файл к заметке», при этом скрепка в режиме заметки скрыта, а если приложить файл до переключения — заметка отправится, файл молча пропадёт.

<sub>Composer.tsx — переключатель «Сообщение | Заметка», поле и кнопка перекрашиваются; MessageBubble.tsx — жёлтая вставка «Заметка · Имя · видно только сотрудникам»; отдельная секция заметок в карточке клиента (card/ClientCardPane.tsx). Но app/api/routes/messages.py:49 — схема NoteIn состоит из text и client_message_id, поля attachments в ней нет, и app/services/messages.py, create_note жёстко ставит attachments=[].</sub>

**Шаблоны быстрых ответов**

Сама вставка сделана правильно (черновик не затирается). Но пикер не закрывается щелчком мимо — только Esc или повторным нажатием ⚡ (обработчика клика снаружи в файле нет), и пока он открыт, он закрывает собой конец переписки.

<sub>composer/TemplatePickerPopover.tsx — открытие по «/» в пустом поле и по кнопке ⚡, поиск, группы «ОБЩИЕ / МОИ», выбор стрелками, Enter — вставить; Composer.tsx, insertTemplate вставляет в позицию курсора и подставляет имя клиента, имя менеджера, объявление. Управление шаблонами — features/templates/TemplatesManager.tsx.</sub>

**Горячие клавиши рабочего места**

Три беды в одном месте. Ctrl+T («Передать») обещан в шпаргалке, срабатывает и не делает ничего. Ctrl+Backspace — привычное в Windows «удалить слово» — вместо этого отклоняет диалог. Перебор диалогов клавишами не прокручивает список: с шестой карточки подсветка уходит за нижний край, а дальше 50-го диалога клавиатурой не уйти вовсе.

<sub>features/hotkeys/useChatHotkeys.ts — J/K и стрелки по списку, Alt+1..4 срезы, Alt+↑/↓ по непрочитанным, Ctrl+R принять, Ctrl+D закрыть, Ctrl+Shift+N заметка, Esc «луковицей», Ctrl+K поиск. Проверено отдельно: transferNonce из actionBus.ts не читает ни один файл во всём frontend/src — Ctrl+T поднимает счётчик, и на этом всё. Ctrl+Backspace обрабатывается до проверки «человек печатает», то есть срабатывает прямо из поля ввода. В ChatListPane.tsx нет ни одного вызова scrollToIndex.</sub>

**Уведомление «Клиент ждёт вашего ответа» в колокольчике**

Главное уведомление смены приходит без иконки (общий 🔔) и без человеческой подписи — в журнале оно выглядит технической строкой «conversation.awaiting_you», и в фильтре по типу его нет, пока такая строка не попадёт в загруженную страницу. То же с «Диалог никто не принял» и «Очередь разгружена».

<sub>Сервер шлёт его адресно ответственному: app/services/notifications.py:260 conversation.awaiting_you, отправка — app/scheduler/jobs/awaiting.py:156. Сверил каталоги: в frontend/src/features/notifications/catalog.ts из двадцати серверных видов отсутствуют три — conversation.awaiting_you, conversation.unclaimed и inbox.cleaned.</sub>

**Тема оформления: тёмная, светлая, авто**

Переключатель работает, но в светлой теме шкала ожидания в списке — бледно-жёлтая цифра на белом. То есть главный сигнал смены «клиент ждёт 20 минут» в светлой теме почти не виден.

<sub>features/settings/profile/AppearanceBlock.tsx — переключатель на три положения. Но в app/lc-vars.css янтарные токены --lc-warn, --lc-on-warn, --lc-warn-soft, --lc-warn-text объявлены только в тёмной теме (строки 247–251); блок светлой темы (строка 426) их не переопределяет, а wait-gauge.css пользуется именно ими.</sub>

**Адаптив под узкие экраны**

На типовом ноутбуке 1366 система считает, что места хватает на три колонки, а фактически ленте достаётся примерно 570 пикселей вместо обещанных 600. Плюс шапка левой колонки (заголовок, поиск, вкладки, три фильтра с переносом) съедает у списка заметную часть высоты.

<sub>ChatsPage.tsx + shared/lib/breakpoints.ts — ниже 1360 карточка клиента уезжает в оверлей по кнопке «Клиент», ниже 768 остаётся стек из двух экранов. Пороги считаются через useMediaQuery, то есть по ширине ОКНА, а левая панель приложения занимает ещё 56px (--lc-rail-w в app/lc-vars.css:137).</sub>

**Видно, кто ведёт чужой диалог**

Имя есть, а «за столом ли этот человек» — нет. Заступивший в 21:00 диспетчер видит имена уехавших домой коллег и считает, что это чужая работа.

<sub>ConversationListItem.tsx — имя ответственного выводится во второй строке карточки, если это не я. В app/services/conversations.py, conversation_out ответственный отдаётся как {id, full_name} — признака «в сети» там нет.</sub>

### ✖ Нет

**Неотправленный ответ видно в списке слева**

Отправка упала — карточка в списке выглядит как успешно отвеченная: «Вы: …», ничего красного. Оператор ушёл к следующему клиенту и не вернётся.

<sub>app/services/conversations.py:192 — last_message без delivery_status; components/list/ConversationListItem.tsx ничем не помечает провал доставки. Красный крест есть только в открытом диалоге (thread/MessageBubble.tsx).</sub>

**Вернуть принятый по ошибке диалог обратно в очередь**

Клавиши «Принять» и «Отклонить» стоят рядом. Промахнулся — либо веди чужой канал, либо ищи конкретного человека и передавай лично.

<sub>На сервере ручка готова: app/api/routes/inbox.py:357 POST /conversations/{id}/release → app/services/inbox.py:987. Во всём frontend/src слово release не встречается ни разу — кнопки и вызова нет.</sub>

**Фото и голосовые от клиента в ленте**

В ленте будет пустой пузырь и строчка «📎 undefined · NaN Б» без ссылки. Сейчас не видно только потому, что на проде стоит имитатор Авито, который шлёт один текст. Сломается в день подключения настоящего канала — а фото шильдика в ремонте техники и есть суть обращения.

<sub>app/integrations/avito/adapter.py:174 _extract_attachments кладёт в базу {"type": "image", ...} — без url, name, size и kind; app/services/media.py:338 отдаёт такое как есть; thread/MessageBubble.tsx рисует ссылку по a.url и подпись a.name + размер.</sub>

**Отправить клиенту файл или фото**

Скрепка в поле есть, файл на сервер грузится, но отправить нельзя. Для запчастей «вот такая деталь, смотрите фото» — ежедневная операция. Пока не сделано, скрепку честнее убрать.

<sub>Composer.tsx: attachmentsBlockSending — кнопка отправки гаснет, как только приложен файл и это не заметка, с плашкой «Авито не принимает файлы от нас — сообщение не уйдёт». Отправки вложений в Авито в коде нет вовсе.</sub>

**Плотный режим списка (больше строк на экране)**

На ноутбуке 1366 в списке помещается 4–5 диалогов против примерно 12 в JivoChat. Это прямая причина, по которой команде будет некомфортно после переезда.

<sub>Заготовки лежат: токен --lc-row-h-dense: 52px в app/lc-vars.css:278 и стили .conv-card[data-dense] в wait-gauge.css:87. Атрибут data-dense не выставляет ни один компонент, переключателя в интерфейсе нет. Высота строки при этом задана тремя разными числами: ROW_HEIGHT = 84 в ChatListPane.tsx, height: 78px в chat-list.css (в трёх местах) и --lc-row-h: 76px в токенах.</sub>

## Вход, права, сотрудники

Кто входит, что кому можно, как заводят и увольняют.

✅ 21 · ◐ 8 · ✖ 2

### ✅ Работает

**Вход по email и паролю**

<sub>app/api/routes/auth.py:120 — проверка пароля argon2, единый текст ошибки на «нет такого email» и «неверный пароль»; frontend/src/features/auth/LoginPage.tsx рисует все состояния: неверный пароль, блокировка с обратным отсчётом, «учётная запись отключена», «сервер недоступен».</sub>

**«Запомнить меня»: при следующем открытии пароль не спрашивают**

Токен доступа живёт только в памяти вкладки и нигде не сохраняется — это сознательно.

<sub>app/core/security.py:113 — refresh-токен в httpOnly-куке lc_refresh с ротацией; повторное использование украденной куки рвёт всю цепочку токенов сотрудника. frontend/src/shared/stores/sessionStore.ts:99 при старте делает тихий обмен куки на новый доступ.</sub>

**Выход из системы**

<sub>frontend/src/app/AppLayout.tsx:114 — кнопка выхода; app/api/routes/auth.py:230 гасит куку, отзывает токен и пишет строку в журнал.</sub>

**Защита от подбора пароля**

Порог «30 разных почт с одного адреса» рассчитан на нынешние 13 человек с запасом; при удвоении команды его придётся поднять.

<sub>app/services/login_guard.py — 10 промахов по одной почте за минуту дают 403 с обратным отсчётом; по адресу считаются РАЗНЫЕ почты (порог 30), поэтому утренняя опечатка одного человека не запирает весь офис за общим адресом. Плюс ограничение на входе nginx (docker/nginx/nginx.conf:65, 60 запросов в минуту с адреса).</sub>

**Администратор узнаёт, что сотрудник не может войти**

<sub>app/api/routes/auth.py:143 — при блокировке учётки админу уходит уведомление «Вход заблокирован: имя», ровно одно за окно блокировки; подпись и иконка есть в каталоге фронта frontend/src/features/notifications/catalog.ts:90.</sub>

**«Не помню пароль» со страницы входа**

Ответ сотруднику одинаковый и на существующий, и на выдуманный адрес — так и задумано, чтобы с улицы нельзя было проверить, кто у вас работает.

<sub>frontend/src/features/auth/ForgotPasswordForm.tsx → POST /support/password-reset (app/api/routes/support.py:79). Админу приходит заявка с кнопкой «Выслать новую ссылку», она тут же показывает одноразовую ссылку (app/services/users.py:588, frontend/src/features/notifications/NotificationsPage.tsx:352).</sub>

**У отключённого сотрудника доступ рвётся мгновенно**

<sub>app/services/sessions.py закрывает сразу три двери: отзыв refresh-цепочки, чёрный список действующего токена и команда всем процессам закрыть открытые окна. frontend/src/shared/realtime/WsClient.ts:71 по коду 4403 уводит человека на страницу входа. Проверено интеграционным тестом tests/integration/test_session_revoke.py.</sub>

**Приглашение сотрудника одноразовой ссылкой**

Почтового сервиса нет — ссылку админ передаёт человеку сам. Адрес в ссылке строится из настройки DOMAIN сервера (app/core/security.py:187, по умолчанию chat.partner-lead-centre.ru): если рабочий адрес системы окажется другим, ссылка приведёт не туда.

<sub>app/services/users.py:194 создаёт строку без пароля и выдаёт ссылку на 72 часа; frontend/src/features/settings/team/InviteModal.tsx (имя, email, роль с описанием прав) и OneTimeLinkModal.tsx (ссылка показывается один раз, есть «Скопировать»).</sub>

**Сотрудник ставит себе пароль по приглашению и сразу входит**

<sub>frontend/src/features/auth/InvitePage.tsx — минимум 10 символов, индикатор надёжности, повтор пароля, понятный экран «ссылка устарела»; app/api/routes/auth.py:275 после установки пароля сразу выдаёт сессию.</sub>

**Перевыпуск ссылки и сброс пароля администратором**

Сброс возвращает учётку в состояние «ждёт пароля» и рвёт сессии: до перехода по ссылке человек работать не сможет. Это правильно, но админ должен знать.

<sub>Меню в строке сотрудника: «Выслать новую ссылку» для приглашённого и «Сбросить пароль» для работающего (frontend/src/features/settings/team/TeamMembersTab.tsx:343, api.ts:47); на сервере это две разные ручки (app/api/routes/users.py:179 и :192).</sub>

**Четыре роли с единой матрицей прав**

<sub>app/core/rbac.py — одна таблица «роль → права» на весь бэкенд; app/api/deps.py:75 всегда читает роль из строки в базе, а не из токена. Фронт получает готовый список прав из /auth/me и своей копии матрицы не держит (frontend/src/shared/auth/usePermissions.ts).</sub>

**Смена роли сотрудника действует немедленно**

<sub>PATCH /users/{id} (app/services/users.py:318) — подтверждение в интерфейсе (TeamMembersTab.tsx:436), после смены открытое окно сотрудника переподключается с новой ролью, а кэш прошлой роли на его экране очищается (frontend/src/shared/auth/useRoleUiSync.ts).</sub>

**Система не даёт остаться без администратора**

Если доступ к единственной админской учётке потеряется, восстановление — только правкой базы. Второго администратора стоит завести до выхода команды.

<sub>app/services/users.py:126 — понизить, отключить или удалить последнего администратора нельзя (409); себе роль сменить, себя отключить и себя удалить тоже нельзя. «Последним» считается только тот, кто реально может войти: приглашённый, но не установивший пароль, за страховку не считается.</sub>

**Разделение «имеет права» и «ведёт диалоги»**

Ровно то, что у вас в Jivo настроено отделом «СТАРШИЕ - ЧАТЫ»: администрируют, обращений не ведут.

<sub>Тумблер «Ведёт диалоги» в строке сотрудника (TeamMembersTab.tsx:307); на сервере признак handles_conversations входит в одно общее условие app/services/conversations.py:79, которым пользуются все четыре выборки — очередь, автораздача, операторы канала и «кому передать». Изменение пишется в журнал.</sub>

**Список сотрудников с поиском и фильтрами**

Точка «в сети» — снимок на момент загрузки страницы: живьём она не обновляется, нужно открыть экран заново.

<sub>GET /users (app/api/routes/users.py:138) + frontend/src/features/settings/team/TeamMembersTab.tsx: поиск по имени и почте, фильтр по роли, галочка «показывать отключённых», статус «активен / ждёт пароля / отключён», точка «в сети», постраничный просмотр с честным «1–13 из 13».</sub>

**Отключить и включить сотрудника**

Диалоги отключённого сознательно остаются за ним — их переназначает руководитель руками через фильтр «Менеджер»; окно подтверждения об этом честно предупреждает.

<sub>POST /users/{id}/deactivate и /activate (app/services/users.py:407 и :562) с подтверждением в интерфейсе; включение снимает чёрный список токенов сразу, чтобы человек не упирался в отказ ещё 15 минут.</sub>

**Журнал: кто что сделал и когда**

Под этот экран в базе нет подходящих индексов: есть только по виду события и по объекту, а журнал за период сортируется по времени и считает общее число строк. Пока записей мало, это незаметно; за год работы 13 человек экран начнёт открываться заметно медленнее. Чистки старых записей нет — журнал растёт бессрочно.

<sub>GET /audit-log (app/api/routes/audit.py) + вкладка «Журнал аудита» (frontend/src/features/settings/team/AuditLogTab.tsx): 41 вид событий с русскими подписями, которые считает сервер (app/services/audit.py), фильтры по периоду и сотруднику, детали каждой строки раскрываются. Доступен админу и руководителю, только на чтение.</sub>

**Неудачные попытки входа видны в журнале**

<sub>app/api/routes/auth.py:89 пишет строку на каждую неудачу с причиной и адресом; на залоченной учётке запись делается один раз за окно блокировки, чтобы неавторизованная ручка не стала способом залить журнал с улицы.</sub>

**Пометить клиента как нежелательного**

<sub>POST /clients/{id}/block и /unblock (app/api/routes/clients.py:61) + окно из шапки ленты (frontend/src/features/chats/components/card/BlockClientButton.tsx). Сообщения помеченного приходят и сохраняются, но диалог не встаёт в очередь и никому не звенит — app/services/inbound.py:346. Пометка и снятие пишутся в журнал вместе с причиной.</sub>

**Боты: завести, включить, привязать к каналам**

Ключ ИИ-провайдера сейчас пуст: шаги с ИИ не выполняются, а диалог честно передаётся оператору с причиной «AI недоступен» (app/bots/engine.py:494) — молча бот не пропадает. Учёта расхода на ИИ и дневного потолка в системе нет.

<sub>frontend/src/features/settings/bots/BotsPage.tsx — список с числом шагов, расписанием и диалогами за 7 дней, «Создать бота» из шаблона «Первичный приём», «Дублировать», тумблер «Вкл» без сохранения формы; BotEditor.tsx привязывает бота к каналам Авито. Сервер — app/api/routes/bots.py, включая песочницу для прогона сценария.</sub>

**Первый администратор и аварийная смена роли**

Это ручной путь на сервере, не для заказчика: нужен для первого входа и на случай, если админский доступ потеряется.

<sub>app/cli.py — команды create-admin, set-role и invite на сервере; служебный робот регрессии (домен .local) намеренно спрятан из всех списков и не управляется из интерфейса (app/services/users.py:109).</sub>

### ◐ Наполовину

**Администратор задаёт сотруднику пароль голосом, без ссылки**

Заведение человека рядом с собой по-прежнему требует ссылки: заказчик просил «сказал пароль вслух — работает», и этого пока нельзя.

<sub>Сервер готов: POST /users/{id}/set-password (app/api/routes/users.py:209, app/services/users.py:445) — пароль от 10 символов, все сессии сотрудника рвутся, в журнал пишется «пароль поставил администратор». В интерфейсе к этой ручке нет ни одного обращения: в frontend/src/features/settings/team/api.ts такой функции нет. Тестов на неё тоже нет.</sub>

**Сотрудник сам меняет себе пароль**

Интерфейс и сервер говорят разное. План работ (docs/21, строка 178) предлагает решить это в другую сторону — подтвердить правило «пароли меняет только админ» и убрать ручку с сервера. Решение за вами, но сегодня расхождение видно глазом.

<sub>Сервер умеет: POST /auth/change-password (app/api/routes/auth.py:323) спрашивает текущий пароль и рвёт остальные сессии, есть юнит-тесты tests/unit/test_own_profile.py. Формы нет: в frontend/src/features/settings/profile/ProfilePage.tsx:65 по-прежнему стоит надпись «Пароль меняет администратор — напишите ему формой ниже».</sub>

**Сотрудник сам меняет себе имя**

Человек, заведённый как «Оператор 3», исправить себя не может. Отдельно: имя нужно только внутри системы — клиенту на Авито подпись оператора не уходит вообще (в app/workers/deliver.py имени нет).

<sub>Сервер умеет: PATCH /auth/me (app/api/routes/auth.py:386), меняет только имя, роль и почта остаются за админом, событие пишется в журнал. В интерфейсе формы нет — поиск по frontend/src не находит ни одного вызова этой ручки.</sub>

**Руководитель видит всё, но не может ответить клиенту**

Это записанное решение, а не забытая доделка — код уважает его последовательно. Но заказчик увидит это как ограничение; снятие стоит около двух дней и тянет миграцию, иначе руководителям наутро поедет автораздача.

<sub>app/core/rbac.py:34 — у роли «руководитель» нет права messages:send; в ленте вместо поля ввода честная плашка «Режим просмотра» (frontend/src/features/chats/components/composer/ThreadFooter.tsx:13); сервер отвечает отдельным кодом read_only_role, а не глухим отказом.</sub>

**Отделы у сотрудников**

В LeadChat-PLAN.md раздел 7.4 помечен «✅ ГОТОВО». По коду готова только колонка в списке сотрудников; на экране назначения вместо отдела показывается роль.

<sub>Поле есть в базе (миграция 0011), правится прямо в таблице сотрудников и сохраняется по уходу фокуса (TeamMembersTab.tsx:288). Но дальше оно нигде не используется: фильтра по отделу в списке нет, а экран «Назначить операторов на канал» получает от сервера отдел всегда пустым — app/services/account_operators.py:219 жёстко отдаёт department: null, и интерфейс группирует людей по роли (AssignOperatorsModal.tsx:82).</sub>

**Уволить (удалить) сотрудника**

До появления кнопки надо закрыть дыру: удалённого можно вернуть в строй запросом «включить» — app/services/users.py:562 не смотрит на отметку об удалении, и такой человек войдёт в систему, не появившись ни в одном списке. Вторая оговорка: возврат его диалогов в очередь происходит только в базе, на экранах у людей они появятся после перезагрузки страницы.

<sub>Сервер написан и написан аккуратно: DELETE /users/{id} (app/api/routes/users.py:233, app/services/users.py:493) — имя остаётся в старых диалогах, из всех списков человек исчезает, доступ рвётся, незакрытые диалоги возвращаются в очередь. В интерфейсе кнопки нет: в меню строки сотрудника только «Выслать ссылку/Сбросить пароль», «Отключить» и «Включить» (TeamMembersTab.tsx:340). Ни одного теста на удаление в tests/ нет.</sub>

**Фильтр журнала по виду события**

Такие события попадают в список выбора, только если случайно оказались на открытой странице, и показываются техническим именем вроде «client.blocked». Сами строки журнала при этом читаются по-русски — подпись считает сервер. Фильтр по сотруднику страдает тем же: список имён собирается из тех, кто может вести диалоги, плюс те, кто попал на текущую страницу, поэтому выбрать руководителя, наблюдателя или отключённого удаётся не всегда.

<sub>Выпадающий список строится из словаря frontend/src/features/settings/team/auditActions.ts, а в нём 22 события против 41 на сервере. Отсутствуют, в частности: удаление сотрудника, смена своего пароля и имени, пометка и снятие пометки клиента, отказ оператора от диалога, все четыре события передачи, массовое закрытие зависших, смена настроек распределения.</sub>

**Удалить бота**

Ненужного бота можно только выключить — он навсегда останется в списке.

<sub>DELETE /bots/{id} на сервере есть (app/api/routes/bots.py:450), в frontend/src/features/settings/bots/api.ts соответствующей функции нет, кнопки в интерфейсе тоже нет.</sub>

### ✖ Нет

**Посмотреть, какие каналы ведёт конкретный сотрудник**

Чтобы понять, что ведёт человек, приходится открывать все девять карточек каналов по очереди.

<sub>Есть только «Мои каналы» в собственном профиле (GET /me/channels, frontend/src/features/settings/profile/MyChannelsBlock.tsx). Обратной выборки «каналы этого сотрудника» нет ни на сервере, ни в интерфейсе.</sub>

**Список помеченных клиентов отдельным экраном**

Снять пометку сегодня можно только найдя диалог этого клиента. В LeadChat-PLAN.md раздел 8.3 помечен «✅ ГОТОВО» — по коду готова пометка из диалога, экрана со списком нет.

<sub>Сервер готов: GET /clients/blocked с постраничным выводом, кто и когда пометил и почему (app/api/routes/clients.py:115). В интерфейсе к нему нет ни одного обращения — поиск по frontend/src не находит вызова.</sub>

## Боты и автоматика

Что система отвечает и о чём предупреждает сама.

✅ 32 · ◐ 9 · ✖ 7

### ✅ Работает

**Список ботов: имя, включатель, каналы, расписание, диалогов за 7 дней**

<sub>frontend/src/features/settings/bots/BotsPage.tsx — таблица с колонками «Бот / Вкл / Аккаунты / Расписание / Диалогов/7д» и кнопкой «Дублировать»; app/api/routes/bots.py:244 GET /bots считает диалоги по следу бота в bot_vars за 7 дней. Раздел «Боты» в меню настроек виден только админу (SettingsLayout.tsx:25, право bots:manage только у роли admin в app/core/rbac.py).</sub>

**Включение и выключение бота тумблером**

У диалогов, где бот в этот момент ждал ответа, признак «бот ведёт» остаётся до следующего сообщения клиента — в таблице диалогов такой диалог ещё какое-то время числится за ботом.

<sub>BotsPage.tsx:149 и BotEditor.tsx:223 дёргают POST /bots/{id}/enable|disable сразу, без сохранения формы; app/bots/runtime.py:317 перепроверяет is_enabled перед каждым шагом — выключенный бот замолкает мгновенно.</sub>

**Дублирование бота**

Копия создаётся выключенной и без каналов — это осознанно: у канала может быть только один бот.

<sub>BotsPage.tsx:42 — читает бота и создаёт копию «(копия)» с тем же сценарием и базой знаний, сразу открывает её в редакторе.</sub>

**Редактор сценария: шаги списком, переименование, перемещение, удаление, смена типа, стартовый шаг**

<sub>frontend/.../bots/components/StepCard.tsx — стрелки «выше/ниже», крестик с подтверждением, выбор типа шага с предупреждением «параметры сбросятся», кнопка «Сделать стартовым», редактируемый идентификатор шага; BotEditor.tsx — черновик живёт до кнопки «Сохранить», есть «Отменить изменения» и предупреждение браузера при уходе с несохранённым.</sub>

**Девять видов шагов сценария и форма под каждый**

<sub>app/bots/steps.py:38 перечисляет send, ask, menu, condition, ai_answer, handoff, close, tag, note; app/bots/engine.py:418 исполняет все девять; frontend/.../StepForms/index.tsx даёт форму на каждый вид (SendForm, AskForm, MenuForm, ConditionForm, AiAnswerForm, HandoffForm, CloseForm, TagForm, NoteForm).</sub>

**Проверка сценария перед сохранением: ошибки блокируют, предупреждения — нет**

<sub>app/bots/validator.py:765 — ловит дубли шагов, битые переходы, недостижимые шаги, тупики без выхода, циклы, неизвестные переменные, кривые регулярки, дублирующиеся ключевые слова меню, шаг ИИ без базы знаний; app/api/routes/bots.py:139 отвечает 422 со списком. BotEditor.tsx:323 показывает список, клик по строке ошибки раскрывает и подсвечивает нужный шаг.</sub>

**Песочница «Протестировать»: проверка сценария до сохранения, без единого сообщения клиенту**

Переключатель «AI: настоящий» сегодня отвечает отказом «AI недоступен» — ключа нет. Для отладки графа работает режим «заглушка».

<sub>app/bots/sandbox.py — гоняет тот же движок, всё пишется в базу и в конце тика откатывается (rollback в finally); frontend/.../bots/SandboxDrawer.tsx — чат-симулятор с трассировкой шагов, панелью состояния, счётчиками и кнопкой «⏩ Промотать таймаут» вместо ожидания суток. Можно задать «сейчас ночь» и проверить ночную ветку.</sub>

**Привязка бота к каналам Авито**

Привязка делается только из карточки бота. На экране «Аккаунты Авито» бота не видно вовсе, и выключателя бота на самом канале нет — чтобы снять бота с одного из девяти каналов, надо зайти в бота и убрать канал из списка.

<sub>app/api/routes/bots.py:402 PUT /bots/{id}/accounts — список каналов целиком, канал, занятый другим ботом, молча перепривязывается; BotEditor.tsx:232 — поле «Аккаунты Авито» с предупреждением «сейчас за другим ботом, при сохранении перепривяжется».</sub>

**Расписание бота: круглосуточно или по интервалам, включая ночные через полночь**

Часовой пояс не выбирается — всегда московский. Расписание — фильтр только на вход: начатый вечером диалог бот доводит до конца и ночью, иначе клиент, ответивший в 10:01 на ночной вопрос, остался бы без реакции.

<sub>app/bots/schedule.py — интервалы по дням недели, start>end читается как интервал через полночь, неизвестная таймзона не роняет тик; frontend/.../components/ScheduleEditor.tsx — галочки дней, поля времени, подпись «через полночь», защита от удаления последнего интервала.</sub>

**Мгновенное «позовите оператора» по словарю, без ИИ**

Работает без ИИ и стоит первым — клиент, попросивший человека, получает его немедленно.

<sub>app/bots/steps.py:376 — словарь «оператор, менеджер, человек, живой, позовите, соедините, хватит бот, не бот»; app/bots/engine.py:234 проверяет его на КАЖДОМ входящем и сразу передаёт диалог человеку.</sub>

**Телефон из переписки попадает в карточку клиента**

Работает без ИИ и бесплатно. В промпт модели номер не уходит — телефоны маскируются (app/bots/steps.py:249).

<sub>app/bots/engine.py:709 capture_phone — регулярка на каждом входящем, номер нормализуется в +7XXXXXXXXXX, записывается в карточку клиента, если там пусто, и в журнал ложится строка client.phone_captured.</sub>

**Передача диалога оператору со сводкой «что бот успел собрать»**

Три из десяти причин сегодня недостижимы — они завязаны на ИИ (негатив, низкая уверенность). Зато причина «AI недоступен» срабатывает всегда, и каждый такой диалог получает заметку об этом.

<sub>app/bots/handoff.py:219 — снимает бота, возвращает диалог в очередь «Входящие» со свежим временем ожидания, пишет заметку «🤖 Бот передал диалог оператору. Причина: … Собрано ботом: Телефон, Проблема, Модель», системную строку в ленту, вешает метки и пишет в журнал. Десять причин перечислены в HANDOFF_REASONS.</sub>

**Метки на диалог от бота**

Поставить метку руками нельзя ни оператору, ни руководителю — ни кнопки, ни серверной ручки нет. Подсказка в фильтре таблицы диалогов («метки ставят и боты, и операторы», TablePage.tsx:384) сегодня неверна.

<sub>app/bots/engine.py:560 exec_tag и app/bots/handoff.py:152 — метки без дублей, событие в браузер сразу; ClientCardPane.tsx:318 показывает их в карточке; ConversationListItem.tsx:36 подсвечивает диалог с меткой «негатив».</sub>

**Бот замолкает навсегда, как только в диалог написал оператор**

Внутренняя заметка вмешательством не считается — бот продолжит вести диалог. Нажатие «Принять» бота тоже не глушит: он замолкает именно от сообщения клиенту.

<sub>app/bots/runtime.py:184 mute_bot — признак «замолчал» кладётся в диалог и не сбрасывается ничем, даже когда клиент возвращается в закрытый диалог; вызывается из app/services/messages.py:407 при каждом исходящем оператора.</sub>

**Диалог, который ведёт бот, одновременно виден во «Входящих»**

Так задумано: любой из тринадцати может перехватить диалог у бота, просто ответив. Но выглядит это непривычно — в очереди появляется обращение, в котором бот уже поздоровался и что-то спросил.

<sub>app/services/inbound.py:265 ставит новому диалогу время входа в очередь, и там же (строка 409) будится бот — оба механизма запускаются на одном и том же сообщении.</sub>

**Предохранители от зацикливания и болтливости бота**

<sub>app/bots/steps.py:61 Limits — не больше 100 шагов на диалог, 20 за один тик, 5 сообщений бота подряд, 2 сообщения клиента «мимо сценария»; app/bots/engine.py:388 при срабатывании отдаёт диалог человеку и пишет ошибку в лог. Валидатор дополнительно ловит статические циклы ещё при сохранении.</sub>

**Ожидание ответа клиента с таймаутом и переспросом**

<sub>app/bots/engine.py:438 ставит отложенную задачу с токеном; app/bots/runtime.py:363 bot_ask_timeout самоаннулируется, если клиент успел ответить, и переставляет себя, если сработала раньше срока. Невалидный ответ переспрашивается заданное число раз, общий дедлайн шага при этом не продлевается.</sub>

**В журнале видно, кто и когда правил сценарий и почему бот отдал диалог**

<sub>app/api/routes/bots.py:351 пишет bot.updated с номером ревизии и списком изменённых шагов; app/bots/handoff.py:274 — bot.handoff с причиной и шагом; app/bots/runtime.py:209 — bot.muted. Все три вида объявлены в реестре журнала (app/services/audit.py:90).</sub>

**Фильтр «Бот ведёт / Бот выключен» в таблице диалогов**

Показывает текущее состояние, а не «бот участвовал». Диалог, который бот собрал и передал оператору, попадает в «Бот выключен» — посчитать «сколько обращений обработал бот» этим фильтром нельзя.

<sub>frontend/src/features/table/TablePage.tsx:370 и app/services/conversation_table.py:103 — фильтр по признаку bot_active.</sub>

**Колокольчик со счётчиком непрочитанного и панель последних уведомлений**

<sub>frontend/src/features/notifications/NotificationBell.tsx и NotificationPanel.tsx, подключены в AppLayout.tsx:204; app/api/routes/notifications.py — /notifications/unread-count и список. Наблюдателю колокольчик не показывается вовсе (право conversations:manage).</sub>

**Журнал уведомлений с фильтрами по важности, типу, периоду и «только непрочитанные»**

<sub>frontend/src/features/notifications/NotificationsPage.tsx — таблица «Время / Важность / Событие / Что произошло / Действие», фильтры, пагинация, «Отметить все прочитанными»; серверная выборка в app/services/notifications.py:666 с проверкой, что человек имеет право это видеть.</sub>

**Красные плашки критичного поверх экрана, пока не подтвердишь**

<sub>frontend/src/features/notifications/CriticalBanners.tsx — не больше трёх одновременно, дальше «и ещё N»; подключены в AppLayout.tsx:287.</sub>

**Кнопка прямо в уведомлении: «Переподключить» канал, «Выслать новую ссылку» сотруднику**

<sub>app/services/notifications.py:126 реестр действий, обе цели написаны (app/services/avito_accounts.py:627 и app/services/users.py); app/api/routes/notifications.py:159 выполняет действие с проверкой прав и отмечает уведомление прочитанным.</sub>

**Повторяющаяся поломка не забивает журнал: одна строка со счётчиком «повторялось N раз»**

<sub>app/services/notifications.py:475 — окна подавления час/6 часов/сутки по важности, счётчик повторов, срок считается от последнего повтора. Повтор, ставший ГРОМЧЕ прежнего (например «сертификат истекает» стало «истёк»), пробивается сквозь подавление и доезжает до браузера.</sub>

**Сторож «Приём сообщений остановился»**

Работает только с 9 до 21 по Москве и не раньше, чем пройдёт полчаса самого рабочего дня — иначе каждое утро приходило бы «приём остановился» по итогам спокойной ночи. Смотрит на приём целиком, а не по каждому из девяти каналов: остановка одного канала при живых остальных не заметится.

<sub>app/scheduler/jobs/watchdog.py:164 — раз в 5 минут; если есть подключённый канал, а входящих от клиентов нет 30 минут, шлёт критичное уведомление админам.</sub>

**Сторож «Очередь входящих не разбирается»**

<sub>app/scheduler/jobs/watchdog.py:251 — берёт то же число, что показывает страница здоровья системы, и при превышении порога шлёт «В очереди N сообщений, самое старое ждёт M мин».</sub>

**Сторож «На диске мало места»**

<sub>app/scheduler/jobs/watchdog.py:298 — предупреждение с 85%, прямой текст «это уже край» с 95%; в docker-compose.prod.yml:125 нужный раздел смонтирован в контейнер планировщика, иначе мерился бы не тот диск.</sub>

**Сторож «Сертификат сайта истекает»**

Без этого тома проверка молчит совсем — то есть на любом сервере, развёрнутом мимо прод-конфигурации, о протухающем сертификате никто не узнает.

<sub>app/scheduler/jobs/watchdog.py:365 — раз в сутки в 9:15 по Москве читает дату из файла сертификата; истёкший сертификат поднимается до критичного. Том /etc/letsencrypt смонтирован в планировщик (docker-compose.prod.yml:120).</sub>

**Сторож «Резервной копии нет»**

Работает только на боевом сервере и только если скриптам задан служебный токен. В плане (LeadChat-PLAN §5.2) записано, что на проде токен не был задан вовсе и все сигналы от скриптов молча получали отказ — проверить, что его вписали, стоит своими глазами.

<sub>app/scheduler/jobs/watchdog.py:449 — ждёт отметку от ночного скрипта; нет её дольше 26 часов, значит копия не делалась, и это критичное уведомление. Отметку ставит deploy/backup.sh через служебную ручку POST /internal/notify.</sub>

**Внешний наблюдатель: «Система не отвечает снаружи»**

Никого не разбудит ночью: уведомление кладётся в тот же центр, который лежит вместе с упавшим сервером. Смысл — след аварии и доклад о простое, когда система вернулась.

<sub>app/api/routes/internal.py — закрытый список видов событий, служебный токен сравнивается за постоянное время, пустой токен означает «ручка выключена, отказ всем»; вид system.unreachable шлёт второй сервер.</sub>

**Старые уведомления удаляются сами через 90 дней**

<sub>app/services/notifications.py:872 cleanup_expired; задача зарегистрирована в планировщике (app/scheduler/main.py:173). Просроченное не показывается, даже если чистка ещё не отработала.</sub>

**Настройка автораздачи: включатель и потолок диалогов на оператора**

По умолчанию выключено осознанно: в Jivo автораздачи нет, и день переезда не должен начинаться с того, что диалоги вдруг стали приходить сами.

<sub>frontend/src/features/settings/distribution/DistributionTab.tsx — тумблер, поле потолка, «Без ограничения», сохранение по кнопке с пометкой «есть несохранённые изменения»; app/api/routes/settings.py:58 пишет всю пачку целиком и кладёт в журнал «было / стало».</sub>

### ◐ Наполовину

**Создание бота одной кнопкой из шаблона «Первичный приём»**

Бот, созданный кнопкой, через сутки молчания клиента сам закроет диалог — вопреки записанному решению «автозакрытия нет». И он приходит с чужими ценами в базе знаний: если ИИ включат, эти цифры бот будет называть клиентам как ваши. Перед включением базу знаний надо переписать, а шаг автозакрытия — убрать.

<sub>Кнопка «Создать бота» шлёт свой шаблон из frontend/.../bots/defaultScenario.ts, а не серверный app/bots/scenarios/primary_intake.json. Шаблоны расходятся в двух местах: в интерфейсном шаг ask_problem.on_timeout ведёт на close_silent (бот сам закрывает диалог через сутки молчания), в серверном — на handoff_no_reply, и в app/bots/scenarios/__init__.py прямо записано «Решение владельца: автоматического закрытия по таймауту НЕТ». Второе расхождение — база знаний: серверная пустая осознанно («выдумывать цены за владельца нельзя»), интерфейсная содержит выдуманные цифры «Замена экрана iPhone — от 8900 ₽», «диагностика бесплатная», «работаем 10:00–20:00».</sub>

**Удаление бота**

Сервер это умеет, кнопки нет. Ненужный бот останется в списке навсегда.

<sub>app/api/routes/bots.py:450 — DELETE /bots/{id} написан, с защитой: привязанного к каналам бота удалить нельзя (409 со списком каналов). В frontend/src/features/settings/bots/api.ts функции удаления нет вовсе, кнопки в интерфейсе тоже нет.</sub>

**База знаний бота (тексты цен и сроков для ИИ-ответа)**

Записать базу знаний можно, применить её нечем до того, как заработает ИИ. Валидатор при этом честно предупреждает «шаг ИИ без базы знаний».

<sub>Поле есть и сохраняется: BotEditor.tsx:253 со счётчиком до 20 000 знаков, app/schemas/bots.py:144 проверяет длину, app/bots/ai.py:397 подставляет её вторым блоком в промпт. Но единственный её потребитель — шаг ai_answer, а он сегодня не работает (нет ключа ИИ).</sub>

**Автораздача и бот не работают вместе**

Сегодня автораздача выключена по умолчанию, и бот работает. В день, когда вы включите автораздачу, бот замолчит на всех обращениях, для которых нашёлся свободный оператор, — нигде об этом не сказано.

<sub>app/services/inbound.py:352 — если автораздача нашла свободного оператора, диалог назначается и app/services/inbox.py:261 переводит его в статус «в работе»; app/bots/runtime.py:138 пускает бота только в диалог со статусом «новый». То есть при включённой автораздаче бот не входит в диалог вовсе.</sub>

**Уведомление «Клиент ждёт вашего ответа» — то самое, ради которого всё затевалось**

Приходит, но выглядит технической строкой: общий значок 🔔 вместо иконки, а в фильтре «Тип события» вместо человеческого названия стоит «conversation.awaiting_you». Три новых вида из трёх.

<sub>Сервер шлёт: app/scheduler/jobs/awaiting.py:154, лично ответственному через 15 минут молчания. Но в frontend/src/features/notifications/catalog.ts:74 записи для conversation.awaiting_you нет — как и для «Диалог никто не принял» (conversation.unclaimed) и «Очередь разгружена» (inbox.cleaned).</sub>

**Сторож «Планировщик не подаёт признаков жизни»**

Настоящую смерть планировщика видит только наблюдатель со второго сервера. Сама проверка полезна — она означает «мы потеряли способ узнать, что он умер», — но названию «планировщик не отвечает» не соответствует.

<sub>app/scheduler/jobs/watchdog.py:408 живёт ВНУТРИ самого планировщика — мёртвый процесс собственную проверку не запустит, и это честно написано в шапке файла. Ловит он более узкое: что пропала отметка живости, хотя планировщик жив.</sub>

**«Аккаунт Авито требует переподключения» в центре уведомлений**

Админ, который в момент поломки не смотрел в экран, в колокольчике и журнале ничего не найдёт. Красный статус канала на экране «Аккаунты Авито» при этом виден, так что поломка не теряется совсем — но приходит не туда, где её ищут.

<sub>Вид события объявлен критичным, с кнопкой «Переподключить», и сама кнопка написана (app/services/notifications.py:179, app/services/avito_accounts.py:627). Но строку этого вида не создаёт никто: по всему app/ поиск needs_reauth находит только мгновенное событие в браузер (app/services/avito_accounts.py:234, app/workers/deliver.py:204) — записи в журнал уведомлений не появляется.</sub>

**Настройка ночной автоуборки очереди: включатель и срок молчания**

Уборка включена по умолчанию и каждую ночь в 7:20 закрывает непринятые обращения, в которых клиент молчит больше 30 дней. Ни изменить срок, ни выключить её из интерфейса нельзя — только правкой на сервере.

<sub>Настройки объявлены и проверяются (app/services/app_settings.py:46 CLEANUP_ENABLED, CLEANUP_DAYS от 7 до 365), ночная задача их читает (app/scheduler/jobs/cleanup.py:66). Но app/api/routes/settings.py отдаёт наружу ТОЛЬКО автораздачу — ручки на уборку нет, экрана тоже нет.</sub>

**Уведомление после каждой ночной уборки очереди**

Приходит и текст человеческий, но в журнале строка идёт под общим значком 🔔 и без названия события — как и «Клиент ждёт вашего ответа».

<sub>app/scheduler/jobs/cleanup.py:80 — «Закрыто обращений: N. Их никто не взял, и клиент молчал дольше M дн. Если клиент напишет снова, диалог вернётся в очередь сам». Но вид inbox.cleaned отсутствует в frontend/.../notifications/catalog.ts.</sub>

### ✖ Нет

**ИИ-ответ клиенту по базе знаний**

Механика написана целиком и аккуратно (структурированный ответ, порог уверенности, обрезка длины, предохранитель на 10 отказов за 5 минут, телефоны маскируются перед отправкой). Не хватает ровно одного — работающего поставщика ИИ. Пока его нет, каждый диалог с шагом ИИ получает служебную заметку «AI недоступен».

<sub>app/core/config.py:88 — anthropic_api_key пустой по умолчанию; app/bots/ai.py:256 без ключа возвращает None, а app/bots/engine.py:492 на None пишет в диалог внутреннюю заметку «🤖 AI недоступен, передал оператору» и отдаёт диалог человеку. В самом коде записано, что с петербургского сервера api.anthropic.com отвечает 403 (app/bots/ai.py:16).</sub>

**Ночной сбор телефона «мастер ответит утром, оставьте номер»**

Самая ценная часть бота для ремонта техники — ночной перехват контакта — сегодня недостижима из-за того, что стоит за шагом ИИ. Лечится перестановкой двух шагов в редакторе, без программиста.

<sub>В шаблоне «Первичный приём» шаг ai_draft стоит ПЕРЕД проверкой рабочих часов (check_hours). app/bots/engine.py:492 при недоступности ИИ передаёт диалог оператору и останавливает сценарий, поэтому шаги night_msg, ask_phone, tag_contact, note_contact и handoff_night не исполняются никогда, пока нет ИИ.</sub>

**Определение недовольства клиента и просьбы «позовите человека» перефразом**

Метка «негатив» и передача оператору по злому сообщению сегодня не срабатывают, кроме случаев, когда клиент употребил слово из словаря (см. следующий пункт).

<sub>app/bots/ai.py:443 classify_message и app/bots/engine.py:666 classify_and_react написаны, но работают через тот же недоступный ИИ; без ключа возвращают None и молча ничего не делают.</sub>

**Учёт расхода на ИИ и дневной потолок трат**

Пока ключ пуст, денег не тратится. В день, когда ключ впишут, счёт станет неограниченным и невидимым — при десяти тысячах сообщений в сутки это сюрприз в конце месяца.

<sub>В app/bots/ai.py нет ни чтения расхода токенов из ответа модели, ни счётчика, ни ограничения; в app/services/stats.py карточки расхода нет. Подтверждается и планом работ (docs/21-ROADMAP §4).</sub>

**«Клиент недоволен» — уведомление руководителю**

Даже когда ИИ заработает, руководитель о недовольном клиенте уведомления не получит — только метка на диалоге.

<sub>Вид conversation.negative объявлен в каталоге (app/services/notifications.py:248) с адресатом «руководитель», но во всём app/ ни один вызов его не порождает. Движок бота при негативе только вешает метку и передаёт диалог (app/bots/engine.py:688).</sub>

**Ещё три объявленных вида уведомлений, которых не бывает**

Каталог обещает больше, чем система шлёт. Из пятнадцати объявленных видов реально приходят одиннадцать.

<sub>«Сообщения не уходят клиентам» (delivery.failures), «AI временно недоступен» (ai.unavailable) и «Клиент вернулся в закрытый диалог» (conversation.reopened) объявлены в каталоге app/services/notifications.py, но ни одного вызова, который бы их создавал, в app/ нет — по conversation.reopened пишется только строка журнала аудита.</sub>

**Пороги сторожей и рабочие часы проверок из интерфейса**

Поменять «считать тишиной 30 минут» или «рабочий день с 9 до 21» можно только правкой файла на сервере с перезапуском.

<sub>app/scheduler/jobs/watchdog.py:122 читает пороги только из окружения (watchdog_inbound_stall_minutes, watchdog_work_start_hour и прочие); в app/services/app_settings.py объявлены всего четыре настройки, и ни одна из них не про сторожей.</sub>

## Статистика и отчёты

Чем меряется работа канала и человека.

✅ 19 · ◐ 7 · ✖ 9

### ✅ Работает

**Экран «Статистика»: период, канал, сотрудники**

Период не длиннее 366 дней, дальше отказ с подсказкой; выбранное «завтра» молча обрезается до сегодня. Все даты — по Москве, а не по часовому поясу браузера.

<sub>frontend/src/features/stats/StatsPage.tsx + components/StatsFilters.tsx: пресеты сегодня/вчера/7/30/этот месяц/прошлый месяц/произвольный (shared/lib/period.ts), мультивыбор менеджеров, выбор канала; сервер — app/api/routes/stats.py:49-66 (parse_period, Filters).</sub>

**Статистику видят только владелец/админ и руководитель**

Проверка серверная, не «спрятанная кнопка»: диспетчер получает отказ даже по прямой ссылке, у него есть только свой виджет за сегодня. Наблюдатель не видит ничего.

<sub>app/core/rbac.py:32-58 (stats:all у admin и head, stats:own у manager), app/api/routes/stats.py:43 (весь раздел под правом на уровне роутера), frontend/src/app/router.tsx:126-135 (гард на /stats).</sub>

**Карточки «Новые диалоги» и «Закрыто» со сравнением с прошлым периодом**

Разная свежесть в соседних карточках: «новые» считаются по часовой витрине и отстают до часа, «закрыто» — живое из журнала. На периоде «сегодня» это видно: закрытых может быть больше, чем «новых».

<sub>app/services/stats.py:617-675 (summary + period.previous()), frontend/src/features/stats/components/SummaryCards.tsx:37-55.</sub>

**Карточка «В работе сейчас»**

Сравнения с прошлым периодом нет намеренно — снимок «сейчас» сравнивать не с чем, и это честно показано.

<sub>app/services/stats.py:403-422; SummaryCards.tsx:57-70 — вынесена в отдельную группу «Прямо сейчас» с подписью «не зависит от выбранного периода».</sub>

**Скорость первого ответа: медиана и среднее за период**

Диалоги без ответа не портят среднее нулём, но считаются отдельным числом «без ответа» — оно видно в подсказке карточки и в выгрузке. Метрика привязана к автору первого ответа, а не к текущему ответственному: передача диалога её не «переносит».

<sub>app/services/stats.py:312-398 (_FRT_COLUMNS, витрина mv_conversation_stats) + миграция 0004 (первый ответ оператора, не считая заметок и неотправленных).</sub>

**Карточка «Закрыто ботом без оператора»**

Пока бот не включён (ключ ИИ на проде пуст), карточка всегда покажет 0 % — это правда, а не поломка. Заметка оператора участием не считается: клиент её не видел.

<sub>app/services/stats.py:453-509 — берётся последнее закрытие в периоде и проверяется, что оператор не написал ни одного сообщения.</sub>

**Карточка «Собрано телефонов» с разбивкой бот / из текста / вручную**

Под фильтром по сотруднику остаются только «вручную»: у телефонов, добытых ботом и автоизвлечением, автора-человека нет, и в разбивке они дадут нули.

<sub>app/services/stats.py:514-548; событие client.phone_captured пишется один раз при первом получении телефона.</sub>

**Карточка «Повторные обращения»**

<sub>app/services/stats.py:553-588 — переоткрытия считаются событиями, повторные клиенты — по витрине.</sub>

**График: 6 показателей, по дням и по часам**

«По часам» доступно только на периоде до 7 дней — кнопка гаснет с объяснением. «Исходящие сообщения» считают только операторов: ответы бота в график не попадают. Пустой день у медианы — разрыв линии, а не ноль; при полном отсутствии данных сетка и ось не рисуются, чтобы пустой график не читался как «ноль».

<sub>app/services/stats.py:680-821 (новые, закрытые, входящие, исходящие, медиана первого ответа, телефоны) + frontend/src/features/stats/components/MetricChart.tsx (своя отрисовка, без библиотеки).</sub>

**Тепловая карта «когда пишут клиенты» (7 дней × 24 часа)**

Фильтр по сотруднику на карту не действует — входящие сообщения сотруднику не принадлежат; на экране это помечено бейджем «все менеджеры», а не умалчивается. Считаются сообщения, а не обращения: один разговорчивый клиент нагревает клетку сильнее, чем пять коротких.

<sub>app/services/stats.py:826-877 (всегда 168 ячеек, кэш 10 минут) + frontend/src/features/stats/components/Heatmap.tsx.</sub>

**Таблица по сотрудникам: принято / отвечено / закрыто / первый ответ / отправлено, со строкой «Итого»**

В одной строке разная свежесть: «отвечено» и время первого ответа — из часовой витрины, «принято», «закрыто» и «отправлено» — живые. Отключённые сотрудники остаются в отчёте, если работали в периоде (помечены «отключён») — отчёт за прошлый месяц не теряет уволенных.

<sub>app/services/stats.py:914-1082 (серверная сортировка по 6 колонкам, «Итого» пересчитывается заново — медиана суммы ≠ сумма медиан) + frontend/.../components/ManagersTable.tsx; клик по строке фильтрует экран, кнопка «Диалоги ↗» уводит в переписки этого человека.</sub>

**Виджет «Моя статистика за сегодня» у диспетчера**

Показывается только роли «диспетчер»: у администратора, который тоже отвечает клиентам, этого виджета нет. Обновляется раз в минуту, на узком экране — пунктом меню. Здесь «ждут моего ответа» посчитано правильно — по последнему сообщению от клиента.

<sub>app/api/routes/stats.py:142-154 (user_id строго из токена, чужое не подставить) + app/services/stats.py:1087-1189; frontend/src/features/stats/MyTodayWidget.tsx, врезан в подвал списка (ChatListPane.tsx:547).</sub>

**Выгрузка статистики в Excel/CSV (листы «Сводка», «Менеджеры», «Диалоги»)**

Ограничения, которые человек встретит: одна выгрузка одновременно, 20 в сутки на человека, не больше 100 000 диалогов (иначе отказ с просьбой сузить период), ссылка живёт сутки, файл удаляется через 7 дней. Лист «Диалоги» строится по часовой витрине — последний час и диалоги, где клиент ещё не писал, в него не попадут. CSV содержит только «Диалоги».

<sub>app/services/stats.py:1298-1354 (постановка в очередь), :1605-1719 (фоновая задача, лист «Диалоги» — 17 колонок), frontend/.../components/ExportModal.tsx + hooks/useStatsExport.tsx (тост с прогрессом и ссылкой). Выгрузка пишется в журнал аудита (stats.exported) — видно, кто и когда выгрузил телефоны клиентов.</sub>

**Защита ссылки на файл выгрузки**

На этом сервере ключ подписи задан (43 знака, проверено 8 августа) — ссылки подписаны по-настоящему. Что остаётся: НИЧТО не проверяет замену ключа при переезде на новый сервер, и там подпись молча посчитается от пустой строки. Задача #36 — про эту проверку, а не про сегодняшний прод.

<sub>Подпись — app/services/media.py:290-299, проверка на входе — docker/nginx/templates/leadchat.conf.template:188-193. Умолчание в app/core/config.py:108 пустое, но на боевом сервере значение подставлено.</sub>

**Таблица диалогов «Диалоги» с метриками (время первого ответа, длительность, число сообщений)**

Метрики считаются только для видимой страницы — иначе первая страница за год считала бы время ответа для 400 тысяч диалогов. Листать глубже 5000 строк не даёт: просит уточнить фильтр вместо молчаливого торможения. Время в колонке — московское и подписано «(МСК)», как и в файле выгрузки.

<sub>app/services/conversation_table.py:130-193, 315-440 + frontend/src/features/table/TablePage.tsx; доступ по тому же праву, что и статистика (app/api/routes/conversations.py:55).</sub>

**Фильтры таблицы диалогов: период, статус, канал, оператор, бот, метка**

Фильтр «Метка» применять не к чему: метки ставит только бот, руками их не поставить (подтверждено аудитом). Подсказки меток собираются из уже загруженной страницы — списком они не притворяются, ввести можно любую.

<sub>app/services/conversation_table.py:95-116 + frontend/src/features/table/TablePage.tsx:319-434; произвольный период перебивает пресет, конец диапазона включительно.</sub>

**Выгрузка таблицы диалогов в CSV**

Потолок 10 000 строк — дальше отказ с просьбой сузить фильтр, и текст этого отказа приходит с сервера. Время в файле московское — совпадает с экраном. «Ещё не ответили» печатается пустой клеткой, а не нулём, чтобы Excel не занижал средние.

<sub>app/services/conversation_table.py:238-294 — те же фильтры и сортировка, что на экране, 11 колонок, формат для русского Excel; frontend/src/features/table/api.ts:82-99.</sub>

**Неделя канала прямо в карточке канала (всего / без ответа + столбики по дням)**

Три оговорки. Первая: цифры из часовой витрины, то есть отстают до часа — сервер даже отдаёт поле «на сколько минут отстаёт», но на карточке оно не выводится. Вторая: с полуночи до 3 часов ночи по Москве сегодняшний день выпадает из столбиков (дни собираются по UTC, а сообщения раскладываются по Москве), хотя в общее число он входит. Третья: если витрина не посчитана, блок просто исчезает — экран каналов при этом работает.

<sub>app/services/account_stats.py (один запрос на все девять каналов) + app/api/routes/avito_connect.py:314-323 + frontend/src/features/settings/accounts/WeekBars.tsx. Сделано 7 августа.</sub>

**Журнал аудита как отчёт: кто что сделал и когда**

Доступен админу и руководителю, только на чтение. Детали события раскрываются по клику как есть — техническим текстом. Журнал переживает удаление сотрудника: имя автора может пропасть, строка останется.

<sub>app/api/routes/audit.py (фильтры по сотруднику, действию, объекту и датам по Москве, страницы по 50) + app/services/audit.py:168-220 (человеческие описания вроде «Статус диалога: В работе → Закрыт») + frontend/src/features/settings/team/AuditLogTab.tsx.</sub>

### ◐ Наполовину

**Карточка «Ждут ответа сейчас»**

Число и подпись расходятся. Клиент, который ждёт ответа у занятого оператора (главный сценарий смены), в эту карточку не попадает вовсе — попадает только тот, кого никто не взял. Правильное определение реализовано, но в другом месте — в виджете диспетчера.

<sub>app/services/stats.py:403-422 — считается count(status='new'), то есть только непринятые из очереди; frontend/src/features/stats/components/SummaryCards.tsx:71-79 — подсказка обещает «диалоги в работе, где последнее сообщение — от клиента».</sub>

**Скорость первого ответа «в рабочие часы»**

При 12-часовых сменах цифра врёт в пользу ночной: клиенту, написавшему в 20:05 и отвеченному в 10:05 утра, засчитывается 5 минут. Поменять часы из интерфейса нельзя, а после загрузки годовой истории смена часов потребует перестройки витрины на боевом объёме (в плане это отдельный пункт).

<sub>app/services/stats.py:76-78 (WORK_START=10ч, WORK_END=20ч) и app/db/migrations/versions/0004_stats_indexes_and_mv.py:52-53 — те же часы зашиты в базу; в app/services/app_settings.py таких настроек нет; frontend SummaryCards.tsx:95 показывает человеку «10:00–20:00».</sub>

**Сортировка в таблице диалогов**

По времени первого ответа, длительности и числу сообщений отсортировать нельзя — это осознанное решение (иначе отчёт считал бы метрики по всей выборке), и заголовок честно не кликается. Но вопрос «покажи худшие по скорости ответа» этой таблицей не решается: только глазами по странице. Ещё две разрешённые сервером колонки (обновлён, непрочитанные) в таблице просто не показаны.

<sub>app/services/conversation_table.py:70-75 — разрешены только last_message_at, updated_at, status, unread_count; frontend/src/features/table/api.ts:107 и TablePage.tsx:466-490 — кликаются ровно два заголовка: «Статус» и «Последнее (МСК)».</sub>

**Ежечасный пересчёт витрины статистики**

Проектный расчёт делался на «десятки тысяч диалогов», а после загрузки истории будет 400 тысяч диалогов и почти 3 миллиона сообщений — уложится ли пересчёт в две минуты, никем не измерено. Если не уложится, обновление молча падает в лог: тревога никому не уйдёт (каналы тревог на проде пока отключены), а вся статистика замрёт на последнем удачном часе.

<sub>app/scheduler/jobs/stats.py:51-91 — задача зарегистрирована на HH:05 и на 03:30 (уборка старых файлов выгрузок), выполняется в режиме, не блокирующем дашборд. Но app/services/stats.py:1725 ограничивает пересчёт 120 секундами, а витрина каждый раз пересобирается целиком по всем диалогам (миграция 0004).</sub>

**Подпись «Данные на 14:05» — честная метка свежести**

Если пересчёт ни разу не проходил (метки в Redis нет), надпись не показывается вовсе — и человек не знает, что смотрит на данные неизвестной давности. Молчание тут выглядит как «всё свежее».

<sub>app/api/routes/stats.py:69-70 (метка в каждом ответе) + frontend/.../StatsFilters.tsx:133-139 и lib/format.ts:54-59.</sub>

**Скорость раздела статистики на боевом объёме**

Сегодня, на пустой базе, открывается мгновенно. После загрузки годовой истории — а она приедет в первую же неделю подключения боевого Авито — экран будет открываться заметно дольше, и это увидит именно руководитель.

<sub>app/services/stats.py:604-622 — сводка выполняет 13 тяжёлых запросов подряд (по 6 на текущий и прошлый период плюс снимок), среди них подсчёт повторных клиентов с проверкой всех прошлых диалогов каждого. Подтверждено аудитом (docs/20) и вынесено в план отдельным пунктом.</sub>

**Выбор события в фильтре журнала**

Двадцати событий в списке нет — выбрать их можно, только если такая запись случайно попала на текущую страницу. Сами записи в журнале при этом видны и подписаны по-человечески: неполон именно фильтр.

<sub>frontend/src/features/settings/team/auditActions.ts — в выпадающем списке 21 подпись, вписанная руками; app/services/audit.py пишет 41 вид событий (отказ от диалога, передача и её отмена, возврат в очередь, удаление сотрудника, блокировка клиента, ручное обновление токена, смена настроек распределения…).</sub>

### ✖ Нет

**Скачать готовую выгрузку повторно**

Закрыл тост или обновил страницу — файл найти нечем, хотя он лежит на сервере ещё 7 дней. Нужна выгрузка заново, а это одна из 20 суточных попыток.

<sub>frontend/src/features/stats/hooks/useStatsExport.tsx — ссылка приходит один раз всплывающим сообщением и хранится только в памяти вкладки; в app/api/routes/stats.py есть только статус по номеру задачи, списка выгрузок нет.</sub>

**Поиск по таблице диалогов**

Расхождение с документами: в описании таблицы значатся «восемь фильтров, включая поиск». Фактически фильтров шесть, а поиск на сервере — пустышка, которая ничего не сужает. Найти диалог по имени клиента или телефону можно в рабочем списке чатов, но не в отчёте.

<sub>app/api/routes/conversations.py:247,272 — параметр q принимается и передаётся в фильтры, но app/services/conversation_table.py:95-116 (_conditions) его не использует ни разу; поля поиска нет и в TablePage.tsx.</sub>

**Посмотреть списком, кому не ответили и кто дольше всех ждёт**

Цифру видно, а нажать на неё и получить список этих обращений нельзя. Для правила «15 минут — и клиент ушёл» это главный разбор смены, и его сейчас делать нечем.

<sub>Числа есть — «без ответа» в подсказке карточки (app/services/stats.py:316-317) и «без ответа» в неделе канала (app/services/account_stats.py:70-73), — но в app/services/conversation_table.py нет ни фильтра «без ответа»/«ждут», ни сортировки по времени ожидания.</sub>

**Выгрузка журнала аудита в файл**

Разбор «что было в ночную смену» делается только листанием по 50 строк на экране.

<sub>app/api/routes/audit.py — единственная ручка на чтение, экспорта нет; в AuditLogTab.tsx кнопки выгрузки тоже нет.</sub>

**Чем закончилось обращение (выезд назначен / отказ / не наш профиль / спам) и сумма заказа**

Самый дорогой пробел участка: на вопросы «сколько обращений стали выездами», «какой канал окупается», «кто из тринадцати приносит деньги» ответа нет и задним числом не соберётся — данные надо начинать писать до того, как сядут люди.

<sub>Поля результата нет ни в модели диалога (app/models/conversation.py), ни в витрине статистики (миграция 0004 хранит только «когда закрыт» и «кем»); в app/services/stats.py нет ни одной метрики результата.</sub>

**Смена как период в отчётах**

Ночная смена 20:00–08:00 разрезана полуночью на два дня, и «как отработала ночная» не спрашивается никак. Кто был на смене, косвенно видно по входам-выходам в журнале, но отчёта такого нет.

<sub>app/services/stats.py:94-159 — период всегда целые сутки по Москве; почасовая разбивка есть только у графика и только до 7 дней; в тепловой карте нет разреза по людям.</sub>

**Отчёты «неделя сотрудника», воронка обращений, отчёт по объявлениям**

В плане работ это отложенный хвост: все три строятся на данных, которых пока нет в достаточном объёме. Разумно спросить после первой недели работы смены.

<sub>В app/api/routes/stats.py пять ручек: сводка, график, карта, таблица менеджеров, выгрузка — ни одной из этих трёх. Объявление есть колонкой в таблице и в выгрузке (item_title), но группировки по нему нет.</sub>

**Расход на ИИ-бота в статистике**

Пока ключ ИИ пуст, расхода нет вовсе. В день, когда бота включат на девяти каналах, счёт станет невидимым — на 10 тысячах сообщений в сутки это сюрприз в конце месяца.

<sub>Поиск по app/services/stats.py не даёт ни одной метрики расхода; в app/bots/ai.py есть потолок токенов на один вызов, но учёта израсходованного, дневного лимита и карточки в отчёте нет.</sub>

**Отчёт приходит руководителю сам (по расписанию, в Telegram или на почту)**

За любой цифрой нужно зайти на экран статистики и выбрать период. Ежедневной сводки «вчера: обращений столько, без ответа столько» не приходит никуда.

<sub>В планировщике (app/scheduler/main.py, app/scheduler/jobs/stats.py) есть только пересчёт витрины и уборка старых файлов; среди видов уведомлений (app/services/notifications.py:177+) отчётов нет.</sub>

## Сервер, выкатка, десктоп

Сервер, выкатка, копии, приложение для Windows.

✅ 15 · ◐ 9 · ✖ 9

### ✅ Работает

**Прод развёрнут и открывается снаружи по HTTPS**

Адрес технический — <адрес прода>. Такую ссылку неудобно отправлять сотруднику, и она же зашита внутрь десктопного приложения.

<sub>docker-compose.prod.yml — семь сервисов (api, worker, scheduler, postgres, redis, nginx, certbot), наружу опубликован только nginx; docker/nginx/templates/leadchat.conf.template. Рабочий адрес https://<адрес прода> зашит умолчанием в deploy/deploy.sh:94, deploy/smoke.sh:48, deploy/workstation/ship.sh:43.</sub>

**Сертификат HTTPS и его автопродление**

<sub>docker-compose.prod.yml:250-262 — сервис certbot гоняет renew каждые 12 часов; docker/nginx/entrypoint.d/40-reload-loop.sh перечитывает nginx раз в 6 часов без остановки; срок сертификата отдельно проверяет deploy/healthcheck-alert.sh:124-133 с порогом 14 дней.</sub>

**Защита входа, вебхуков и API от флуда и перебора паролей**

Лимит входа поднят с 10 до 60 в минуту после нагрузочного прогона: офис выходит с одного адреса, и прежний порог отклонял вход своим же людям.

<sub>docker/nginx/nginx.conf:57-78 — зоны limit_req: вход 60 запросов/мин, вебхуки 50/с, API 30/с, плюс ограничение одновременных соединений с одного адреса. deploy/smoke.sh SM-8 проверяет, что вебхук без секрета отдаёт 403.</sub>

**Сервер не разваливается от одного сбойного процесса**

<sub>docker-compose.prod.yml: у всех сервисов restart: unless-stopped, свой healthcheck и потолки CPU/памяти (api 2 CPU/1 ГБ, postgres 2 CPU/2 ГБ и т.д.); логи контейнеров ротируются по 20 МБ (x-logging).</sub>

**Выкатка на прод одной командой, со снимком базы до миграций**

Запускается только с Мака разработчика, где настроен ssh-алиас leadchat. Заказчик выкатить не может, второго человека, умеющего это делать, нет.

<sub>deploy/workstation/ship.sh (169 строк): отказ на грязном дереве → снимок базы через deploy/backup.sh (не снялся — выкатка прекращается) → на сервер едет ровно то, что закоммичено → сборка образов на сервере → alembic upgrade head до подмены кода → перезапуск по одному с ожиданием healthy.</sub>

**Сторож «выкатка не целится в чужую систему»**

Makefile сторож не покрывает: `make smoke` до сих пор ходит на chat.partner-lead-centre.ru (Makefile:66), а `make prod-logs`/`prod-backup` — в /opt/leadchat (Makefile:65), которого на этом сервере нет.

<sub>tests/unit/test_deploy_target.py — семь проверок: ни в одном из пяти скриптов и конвейеров умолчанием не стоит чужой домен, и при этом рабочий адрес назван в каждом (обратная проверка, чтобы сторож не зеленел на пустом месте).</sub>

**Изменения структуры базы применяются при выкатке автоматически**

<sub>17 миграций в app/db/migrations/versions/; ship.sh:118 накатывает их до подмены кода (expand-contract), вниз-миграции запрещены осознанно и это записано. Накат с нуля проверяется на каждом прогоне: tests/integration/conftest.py поднимает настоящий PostgreSQL и делает alembic upgrade head — 181 проверка прошла.</sub>

**Копия базы уезжает в облако**

Хранилище — S3 того же хостера, что и сервер. Отказ на стороне хостера (блокировка аккаунта, сбой региона) уносит и прод, и эту копию разом.

<sub>deploy/backup.sh:163-198 — rclone copy дампа, дополнительная месячная копия первого числа, ротация 30 суточных и 12 месячных; при незаданном RCLONE_REMOTE скрипт громко пишет в лог и шлёт событие backup.offsite_missing. Медиа синхронизируются с предохранителем --max-delete, пустой каталог sync не запускает.</sub>

**Вторая копия базы в другой стране**

Работает. В описи было сказано обратное — она смотрела образец расписания в репозитории, а не боевой сервер.

<sub>Проверено на сервере 8 августа: в crontab стоит `45 3 * * * BACKUP_DIR=/var/leadchat/backups push-offsite.sh`; в журнале за 8 августа — «db_20260808_0030.dump доставлен, sha256 совпал» и «копий на 10.10.0.2 — 4, храним 30 суток». 10.10.0.2 — второй сервер (Амстердам) через wireguard wg0, то есть копия действительно в другой стране и у другого хостера.</sub>

**Проверка «копия действительно читается» и учение по восстановлению**

Проверяется только локальная копия. Та, что уехала в облако, не проверяется никогда. Учение меряет только восстановление базы — «от нуля до рабочего сервиса» ещё не замеряли ни разу.

<sub>deploy/backup-verify.sh поднимает одноразовый postgres, восстанавливает последний дамп, считает пользователей/диалоги/сообщения и требует, чтобы последнее сообщение было не старше двух суток; в расписании стоит на 5-е число. deploy/restore-check.sh — ручное квартальное учение с отчётом (что восстановилось, за сколько, целевой RTO два часа) и чек-листом ручных шагов.</sub>

**Проверка здоровья системы каждые 5 минут**

Живёт на том же сервере, что и система, поэтому смерть самого сервера не заметит. Отдельно: остановку приёма сообщений эта проверка не ловит — app/api/routes/health.py:176-179 считает возраст последнего вебхука, но в списке «красных» признаков (строки 260-288) это число не используется, и ответ остаётся «ok».

<sub>deploy/healthcheck-alert.sh: /api/health, /api/health/deep, срок сертификата, свободное место, свежесть последнего дампа; антидребезг — сообщение при смене состояния и не чаще раза в час. Стоит в расписании (deploy/crontab.leadchat:36).</sub>

**Поломки видны внутри самой системы, в центре уведомлений**

<sub>app/api/routes/internal.py — закрытый список событий с человеческими заголовками, важность выбирает сервер, а не bash; подписи и иконки — frontend/src/features/notifications/catalog.ts. Служебный токен на боевом сервере ЗАДАН (64 знака, проверено 8 августа) — в описи говорилось, что он CHANGE_ME, но это значение из файла-образца, а не с сервера.</sub>

**Система сама замечает шесть видов собственных поломок**

Доходит до человека тем же путём, что и предыдущий пункт — через центр уведомлений, то есть упирается в тот же незаполненный служебный токен.

<sub>app/scheduler/jobs/watchdog.py — приём сообщений встал, очередь не разбирается, кончается диск, истекает сертификат, умер планировщик, устарела резервная копия. Запуск раз в 5 минут и раз в сутки (app/scheduler/main.py). Учитывает рабочие часы, чтобы не присылать «приём остановился» по итогам спокойной ночи. Покрыт tests/unit/test_watchdog.py.</sub>

**Тесты сервера и интерфейса**

Гоняются руками — автоматически запускать негде (см. пункт про CI). Проверки типов и стиля (ruff, mypy, tsc, eslint, clippy) описаны там же и тоже запускаются только руками.

<sub>Прогнал сейчас: tests/unit — 1505 проверок, 3 минуты, все зелёные; tests/integration на настоящем PostgreSQL в контейнере — 181 проверка, все зелёные; интерфейс (npx vitest run) — 55 файлов, 362 проверки, все зелёные. Набор устроен так, что не зеленеет вхолостую: tests/integration/conftest.py на сборке падает, если Docker недоступен, вместо тихого пропуска.</sub>

**Проверка на боевом объёме — 400 тысяч диалогов**

Замер сделан на текущем коде. Но он про объём, а не про поток — поток меряет предыдущий пункт, и он не зелёный.

<sub>tests/load/fill.py заливает объём через COPY с распределением статусов и длин переписки из выгрузки заказчика, помечает свои данные и умеет убирать за собой. Результат зафиксирован в docs/19-AUDIT-2026-08-07.md: 400 013 диалогов, 2 843 223 сообщения, 1,5 ГБ, все экранные запросы укладываются в 200 мс.</sub>

### ◐ Наполовину

**Прод разговаривает с имитатором Авито, а не с настоящим**

Всё, что «проверено на проде», проверено на имитаторе. Первое подключение настоящего Авито — непроверенный шаг, и часть вещей (фото от клиента, годовая история) ломается именно в этот день.

<sub>docker-compose.override.yml:26 поднимает на проде сервис fake-avito; переключение на боевой — две переменные AVITO_API_BASE и AVITO_AUTH_URL (docs/RUNBOOK-DEPLOY.md §9).</sub>

**Проверка живого прода после выкатки**

Выкатка засчитывается удачной, даже если сломан вход, список диалогов, вебсокет или отдача статики. Запустить набор руками можно (make smoke), но эта команда до сих пор целится на чужой домен — Makefile:66.

<sub>ship.sh:156 проверяет ровно одно — что /api/health отвечает «ok». Полный набор из десяти сценариев написан (deploy/smoke.sh, 351 строка, и tests/smoke/test_smoke.py: вход, список диалогов, вебсокет, тракт «запись → реальное время», очередь, планировщик, вебхук без секрета, статика, состояние каналов Авито), но запускается он только из .github/workflows/deploy.yml:169, который не работает.</sub>

**Инструкция по выкатке и эксплуатации**

Человек, который откроет эту инструкцию в момент аварии, пойдёт не в тот каталог и будет искать конвейер, которого нет. Отдельно: проверки «боевые ключи заменены» в deploy/ действительно нет, но сами ключи на этом сервере заданы — это задача #36 про новый сервер, а не про сегодняшний.

<sub>docs/RUNBOOK-DEPLOY.md — 15 разделов, от подготовки сервера до инцидентов. Но §13 описывает выкатку тегом через GitHub CI (её нет), каталог всюду /opt/leadchat, а настоящая выкатка идёт из deploy/workstation/ship.sh в /srv/leadchat — слова «ship.sh» в инструкции нет ни разу. Каталог бэкапов в инструкции /var/backups/leadchat (строка 114), а в .env.prod.example:143 — /var/leadchat/backups.</sub>

**Ночная резервная копия базы**

Сам скрипт хороший. Не подтверждено, что расписание вообще установлено на сервере и указывает в существующий каталог — это проверяется одной командой crontab -l, и до этой проверки нельзя утверждать, что ночные копии есть.

<sub>deploy/backup.sh (205 строк) написан добротно: проверка свободного места, pg_dump, содержательная проверка дампа по оглавлению и ключевым таблицам (а не по размеру файла), предупреждение при резком похудении, ротация 14 суток. Но расписание deploy/crontab.leadchat указывает на /srv/leadchat, а инструкция и конвейер — на /opt/leadchat.</sub>

**Сбор ошибок для разбора инцидентов (Sentry)**

Серверная половина готова и ждёт заведённого проекта. Браузерной половины не существует вовсе, хотя инструкция (RUNBOOK §11, пункт 9 чек-листа приёмки) просит проверить тестовое исключение во фронтенде.

<sub>Сервер: app/core/observability.py — инициализация для api/worker/scheduler с вычисткой секретов из событий, но SENTRY_DSN в .env.prod.example:130 пуст. Интерфейс: docker/Dockerfile.web:27 принимает VITE_SENTRY_DSN и конвейер его прокидывает, но во frontend нет ни пакета @sentry/*, ни единой строки кода — проверено поиском по frontend/src и package.json.</sub>

**Автообновление десктопа**

Ни одного релиза не выложено — обновляться не с чего и не на что. Проверить весь этот тракт можно только после первой настоящей сборки.

<sub>Тракт собран целиком: desktop/src-tauri/src/updater.rs (расписание проверок, отложить на сутки, установка), frontend/src/platform/tauri/updater.ts и неблокирующий баннер в AppLayout.tsx:285, кнопка «Проверить обновления» в AboutAppBlock.tsx:64, раздача /download/latest.json в nginx с белым списком расширений, выкладка deploy/publish-desktop.sh (325 строк) со сверкой манифеста с артефактами, атомарной подменой и проверкой снаружи.</sub>

**Трей, тосты Windows, офлайн-режим, автозапуск, глобальные клавиши**

Всё это на Windows ни разу не выполнялось — только тесты логики на macOS. Плюс два известных ограничения: статус «Отошёл» из трея глушит звук локально и на распределение диалогов не влияет, а глобальных клавиш при свёрнутом окне ровно одна.

<sub>desktop/src-tauri/src/: tray.rs (меню «На месте / Отошёл», число непрочитанных на значке), notify.rs (1294 строки — тосты с быстрым ответом), cache/ (шифрованный кэш и очередь исходящих), lib.rs (автозапуск в трей, ссылки leadchat:// с белым списком разделов). Глобальное сочетание зарегистрировано ровно одно — Ctrl+Shift+L, lib.rs:338-358.</sub>

**Тесты десктопного приложения**

Тест привязан к календарной дате 5 августа: с 6-го числа его курсор считается протухшим, и проверка «после переподключения докачиваем только изменения» падает каждый прогон. Это дефект теста, а не приложения, но красный набор перестают читать — а заметить было некому, потому что CI не подключён.

<sub>Прогнал сейчас: cargo test — 87 зелёных, 1 красный. Падает sync::tests::reconnect_sends_updated_since_cursor (desktop/src-tauri/src/sync.rs:405), причём стабильно, три прогона подряд.</sub>

**Нагрузочный прогон перед запуском**

Два порога из пяти не пройдены. Причина известна и уже починена после того прогона — лимит входа в nginx поднят с 10 до 60 запросов в минуту (docker/nginx/nginx.conf:65), — но прогон после починки не повторяли. То есть подтверждённого зелёного нагрузочного результата на сегодняшнем коде нет.

<sub>tests/load/release.js (k6: 50 входящих вебхуков в минуту со спайком до 250, 30 операторов на вебсокетах) и tests/load/inject.py (серверная половина, чтобы мерить задержку одними часами). Последний записанный результат лежит в summary.json в корне: 31 минута прогона, 11,9% запросов с ошибкой, 787 отказов входа, p95 запроса оператора 985 мс. Объявленные пороги в release.js:170 — доля ошибок меньше 0,1% и p95 меньше 500 мс.</sub>

### ✖ Нет

**Постоянный адрес системы (домен)**

Пока домен не решён, любая ссылка на систему, отправленная сотруднику, открывает чужой хелпдеск, а не LeadChat. От этого же адреса зависят регистрация вебхука в Авито, адрес возврата OAuth и обновление десктопа — менять его потом дороже, чем сейчас.

<sub>deploy/workstation/README.md и tests/unit/test_deploy_target.py: A-запись chat.partner-lead-centre.ru ведёт на <сторонний сервер>, где работает чужой FreeScout с действующим сертификатом на это же имя. Проверено 7 августа.</sub>

**Откат, если выкаченная версия оказалась сломанной**

Строчка «откат выполнен» в выводе появится, но откатывать физически нечего: прежний образ перезаписан. Реальный откат сегодня — это вторая выкатка предыдущего коммита руками, минут двадцать.

<sub>ship.sh:162 при красном health зовёт deploy/rollback.sh → deploy/deploy.sh, а тот на шаге 1/6 делает docker compose pull из GHCR (deploy.sh:191). Реестра нет: образы собираются прямо на сервере под фиксированным именем local/leadchat-api:prod (docker-compose.override.yml:18). Предыдущего образа не остаётся, файл .state/previous_tag на этом пути никто не пишет.</sub>

**Автоматическая сборка и выкатка (CI/CD)**

Работа сделана, но не включена — решение «где хранить код» за вами. Пока его нет: тесты гоняются руками, выкатка держится на одном человеке и одном ноутбуке, а красный тест никто не замечает (см. пункт про тесты десктопа).

<sub>`git remote -v` пуст, тегов нет ни одного. При этом .github/workflows/ci.yml (тесты сервера и интерфейса), deploy.yml (сборка → GHCR → ssh → smoke → откат → уведомление) и desktop-release.yml написаны полностью и требуют только репозитория на GitHub и секретов.</sub>

**Ключ, без которого резервная копия бесполезна, хранится вне сервера**

Потеря сервера вместе с этим файлом даёт живую базу и девять мёртвых каналов. Где хранить копию файла настроек — решение за вами, оно есть в плане работ.

<sub>TOKEN_ENC_KEY живёт в файле .env на самом сервере (.env.prod.example:89); deploy/restore-check.sh:116 честно пишет в чек-листе: без него токены Авито не расшифруются.</sub>

**Тревоги о сбоях приходят живому человеку**

Сейчас все проверки пишут в лог, который никто не читает. Отдельно: в проекте нет ни одного имени и телефона — кого будить ночью и по каким событиям, не решено, а без адресата включать канал бессмысленно.

<sub>Все четыре скрипта (backup.sh:67, backup-verify.sh:31, healthcheck-alert.sh:40, deploy.sh:99) при пустых ALERT_TG_BOT_TOKEN/ALERT_TG_CHAT_ID молча ничего не отправляют; в .env.prod.example:138-139 обе переменные пустые. План работ от 7 августа ставит включение тревог первым пунктом первого рубежа.</sub>

**Внешний наблюдатель: заметить, что сервер умер целиком**

Пока её нет, ночное падение прода не оставляет вообще никакого следа: к утру всё работает и выглядит так, будто ничего не было.

<sub>deploy/watchdog-offsite.sh написан и честно описывает свои пределы, но в deploy/crontab.leadchat:51 строка его запуска закомментирована — по замыслу она должна стоять в расписании амстердамского сервера, а не петербургского.</sub>

**Десктопное приложение: установочный файл для Windows**

Кода написано 3500 строк на Rust плюс мост во фронтенде — но ни разу не собрано в установщик и ни разу не запущено на Windows. Отдельно: в desktop/src-tauri/tauri.conf.json в разделе bundle.windows нет ни certificateThumbprint, ни signCommand — значит в день установки будет тринадцать раз «Windows защитила ваш компьютер, неизвестный издатель» плюс реакция антивируса. И кнопка «Скачать приложение для Windows» на странице входа (LoginPage.tsx:229) сейчас ведёт в 404.

<sub>desktop/README.md прямо говорит: собрать инсталлятор можно только на Windows, а проверено на macOS только cargo check и cargo test. В desktop/src-tauri/target нет ни одного .exe/.msi/.sig. Сборку делает .github/workflows/desktop-release.yml по тегу desktop-v*, тегов нет ни одного.</sub>

**Запасной вход, когда основной сервер недоступен**

Адрес зеркала надо вписать в десктоп ДО первого релиза: чтобы обновиться, приложение качает обновление с того самого сервера, который лежит. Иначе запасной вход покроет ноль из тринадцати диспетчеров.

<sub>Второй сервер (Амстердам, <IP наблюдателя>) используется только как транзит для разработчика — deploy/workstation/README.md, open-leadchat.sh, fix-routes.sh. Зеркала системы для диспетчеров нет. В desktop/src-tauri/tauri.conf.json в connect-src и в plugins.updater.endpoints перечислены только два адреса, амстердамского среди них нет.</sub>

**Проверка самих скриптов выкатки**

Ровно поэтому расхождения путей (/srv против /opt, два разных каталога бэкапов) никто не поймал: их некому ловить.

<sub>В deploy/ около 2286 строк bash (выкатка, откат, бэкап, проверка восстановления, мониторинг, публикация десктопа). Ссылается на них ровно один тестовый файл — tests/unit/test_deploy_target.py, и тот проверяет только адреса. Ни shellcheck, ни bats, ни проверки «пути в расписании совпадают с путями в конвейере» нет.</sub>
