[project]
name = "fake-avito"
version = "0.1.0"
description = "In-memory Avito API mock for LeadChat development and tests (docs/07-TESTING-SECURITY.md §2)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "python-multipart>=0.0.12",
    "httpx>=0.27", # доставка вебхуков на PUBLIC_BASE_URL бэкенда (/_control/incoming)
]
