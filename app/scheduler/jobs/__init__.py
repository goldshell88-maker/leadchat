"""Пакет job'ов планировщика (08 §6).

Каждый модуль экспортирует ``register(scheduler)`` — процесс планировщика
(``app/scheduler/main.py``) вызывает их при старте. Такая раскладка держит
расписание рядом с кодом задачи, а не в одном разрастающемся ``main.py``.
"""
