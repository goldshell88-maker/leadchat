"""Ссылка на профиль клиента на Авито — то, чего не хватало кнопке.

⚠ ПРОСЬБА ВЛАДЕЛЬЦА 02.09: «нужно сделать, чтобы можно было перейти на профиль
авито клиента».

ПОЧЕМУ КОЛОНКА, А НЕ ВЫЧИСЛЕНИЕ НА ЛЕТУ. Кнопка в карточке существует с августа
и рисуется при непустом `profile_url`; отдавала его заглушка, всегда пустая.
Причина заглушки записана в `client_enrich.client_profile_url`: собрать адрес
было не из чего. Единственный идентификатор клиента у Мессенджера — числовой
`author_id`, а публичная страница живёт по другому, непрозрачному ключу, и
преобразования «число → ключ» нет. Значит адрес можно только ПОЛУЧИТЬ от Авито и
запомнить — вывести его из того, что уже лежит в базе, нельзя.

ОТКУДА ОН БЕРЁТСЯ ТЕПЕРЬ. Сторож `_log_peer_fields`, заложенный ради этого
вопроса, показал в боевом логе 02.09, что Авито присылает про собеседника поле
`public_user_profile`. Мы этот блок уже читаем — из него берётся аватар клиента
(`adapter._peer_avatar_url`), и аватары в бою работают. То есть блок приходит и
заполнен; недостающая часть — прочитать в нём адрес профиля.

⚠ ВТОРАЯ КОЛОНКА — ОГРАНИЧИТЕЛЬ, И БЕЗ НЕЁ ПРАВКА СТОИЛА БЫ ВЕЧНОГО НАЛОГА.
Обогащение обязано сходить в Авито у всех, кого завели до этой правки: имя,
объявление и аватар у них давно на месте, и ранний выход сработал бы у каждого.
Но условие «нет ссылки — иди спроси» без памяти о попытке означает поход на
КАЖДОЕ входящее сообщение, вечно, если Авито ссылку не даст. А он может не дать:
форма поля взята из разбора (docs/44 §6) со ссылкой на спецификацию, которой в
репозитории нет, и проверить её нечем. Пятьсот с лишним входящих в сутки — это
пятьсот лишних обращений в чужой API, которые делят окно с доставкой ответов
клиентам (08 §4.3), и ни одно из них ничего не приносит.

`profile_checked_at` делает цену конечной: спросили однажды — записали, что
спрашивали, и больше не ходим. Пустой ответ Авито тоже знание. Понадобится
переспросить (например, если разбор поправят) — это один UPDATE, обнуляющий
колонку, а не выкатка.

NULL ЗАКОНЕН И ОСТАНЕТСЯ ЗАКОННЫМ. У клиентов, заведённых до этой правки, поля
нет, пока обогащение не сходит за ними заново. Пустое значение означает ровно
«ещё не знаем», и карточка в этом случае показывает прежнее — номер клиента на
Авито с кнопкой копирования. Ссылка, ведущая в никуда, хуже её отсутствия:
диспетчер жмёт её при клиенте, на телефоне.
"""

import sqlalchemy as sa
from alembic import op

revision = "0059"
down_revision = "0058"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("clients", sa.Column("profile_url", sa.Text(), nullable=True))
    op.add_column(
        "clients",
        sa.Column("profile_checked_at", sa.DateTime(timezone=True), nullable=True),
    )


def downgrade() -> None:
    op.drop_column("clients", "profile_checked_at")
    op.drop_column("clients", "profile_url")
